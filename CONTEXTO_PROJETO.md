# Z META — Documento de Contexto do Projeto

> **Como usar este documento:** cole o conteúdo abaixo como primeira mensagem em um novo chat com o Claude para retomar o trabalho no Z Meta com contexto completo. Este arquivo vive em `Z Meta/CONTEXTO_PROJETO.md` dentro da pasta do projeto e deve ser mantido atualizado ao final de cada sessão relevante. **Esta versão substitui integralmente qualquer versão anterior deste arquivo** — o projeto evoluiu muito desde os primeiros rascunhos de contexto (redesign do colaborador, tabela `employee_stage_prizes`, "estágios" do mês etc. são histórico morto, já removidos do sistema).

---

## 1. O que é o Z Meta

Z Meta é um SaaS de gestão de equipes de varejo (moda/atacado), multi-tenant, construído por **Felipe dos Santos Ribeiro**, fundador da **FORGE GROUP**. Ele organiza tarefas diárias, metas de vendas em camadas, comissionamento, advertências e premiações de colaboradores, gerentes, supervisores e sócios de empresas-cliente, com um painel de Master Admin no topo para operar o negócio como um todo (faturamento, cobrança por usuário cadastrado, saúde das lojas). O produto é pensado para ser vendido a outras empresas (multi-tenant desde a fundação).

## 2. Stack técnica

- **Frontend/Backend:** Next.js 14 (App Router), JavaScript puro (sem TypeScript).
- **Estilo:** Tailwind CSS. Fonte do app inteiro: **Inter**, auto-hospedada via `@fontsource/inter` (decisão deliberada — `next/font/google` falha no ambiente de build usado pra verificação, que não tem acesso à internet do Google Fonts; `@fontsource` resolve via npm e é mais robusto em produção também, sem dependência de CDN externo em runtime).
- **Backend de dados:** Supabase (Postgres + Auth + RLS + Storage).
  - **project_id:** `fjscwmrjkxgygdzwwrdh`
- **Local do repositório no computador do Felipe:** `/Users/feliperibeiro/Desktop/Z Meta`
- **Deploy:** Vercel a partir do `git push` na branch principal (o Claude nunca roda `git push` — ver seção 7).
- **Login interno:** usuários entram com `username` + senha; internamente vira `email = ${username}@zmeta.local` no Supabase Auth.

## 3. Hierarquia de papéis (roles)

```
master_admin  → dono do Z Meta (Felipe/FORGE GROUP). Vê e opera todas as empresas-cliente.
   └── socio         → dono de uma empresa-cliente. Vê automaticamente TODAS as lojas da empresa (sem precisar de loja_access).
         └── supervisor  → escopo definido por loja_access (linhas explícitas com permission "ver"/"gerenciar").
               └── gerente     → dono de uma equipe dentro de uma loja (profiles.gerente_id aponta pra ele). Uma loja pode ter vários gerentes com equipes diferentes.
                     └── colaborador → nível operacional. Marca tarefas, lança vendas, vê sua própria meta/comissão.
```

Cada papel tem sua própria rota (`/colaborador`, `/gerente`, `/socio`, `/supervisor`, `/admin`), mas os quatro primeiros reaproveitam os **mesmos componentes de experiência completa**:

| Papel | Componente reaproveitado | Onde mora |
|---|---|---|
| colaborador | `ColaboradorView` | `lib/ColaboradorView.js` |
| gerente | `GerenteView` (dentro renderiza `EmpresaDashboard`) | `lib/GerenteView.js` |
| sócio/supervisor | `HierarchyHome` (dentro renderiza `EmpresaDashboard` por loja selecionada) | `lib/HierarchyHome.js` |
| loja (Placar/Colaboradores/Tarefas/Advertências/Premiações/Metas/Lançamentos) | `EmpresaDashboard` | `lib/EmpresaDashboard.js` |

## 4. Padrão "Ver como" (view-as / impersonation)

Usado extensivamente: gerente vê como um colaborador da própria equipe; supervisor/sócio veem como gerente ou colaborador das lojas sob gestão; **master_admin vê como qualquer usuário de qualquer nível**, inclusive sócio/supervisor.

- `ColaboradorView({ profile, tab, viewedByManager, onBack })` e `GerenteView({ profile, tab, viewedBySupervisor, onBack })` recebem o `profile` da pessoa sendo visualizada e devem ser montados com `key={profile.id}` pra garantir estado limpo por pessoa.
- `HierarchyHome({ role, impersonate, viewerProfile, onExitImpersonation })` — quando `impersonate` é passado (só usado pelo master_admin), a tela inteira vira a experiência daquele sócio/supervisor, com um banner dourado "Visualizando como Master Admin" no topo e botão de voltar.

### ⚠️ Padrão de segurança de identidade (crítico — não regredir)

O cabeçalho (`AppShell`) — nome, avatar e username exibidos em "Meu perfil" — **precisa SEMPRE representar quem está de fato logado**, nunca a pessoa sendo visualizada. A rota `/api/account/update-username` resolve o alvo pelo JWT real da sessão (`callerClient.auth.getUser(token)`), não por qualquer id vindo do cliente. Se o AppShell mostrasse a identidade da pessoa impersonada, "editar meu perfil" mudaria silenciosamente as credenciais reais do Master Admin.

Implementação: dentro de `HierarchyHome.js` existem `shellName`, `shellId`, `shellUsername`, `shellAvatarUrl`, `shellOnNameChange`, `shellOnAvatarChange`, que caem para `viewerProfile.*` (e os callbacks viram `undefined`) somente quando `impersonate` é truthy. Em `app/admin/page.js`, todos os `<AppShell>` sempre usam `profile.*` do próprio master (nunca `viewingProfile.*`). **Qualquer nova tela que suporte impersonation precisa seguir esse mesmo padrão.** Esse foi um bug real já encontrado e corrigido numa varredura — não reintroduzir.

## 5. Modelo de metas em camadas (não somam)

As metas de uma loja/mês (`Meta`, `Super Meta`, `Hiper Meta`...) **não são cumulativas**. A meta "em jogo" é sempre o próximo nível ainda não batido; se todos os níveis já foram batidos, vale o último. Calculado por `currentGoalTarget(sortedTotals, sold)` em `lib/scoring.js`, usado em todo lugar que precisa saber "qual é a meta atual" (herocards de colaborador/gerente/sócio, cálculo de comissão). A comissão aplicada é a da **maior camada efetivamente atingida** (`achievedTier`), não da camada "em jogo". Essa foi uma correção crítica feita depois de um bug real (o sistema estava somando os níveis, o que inflava a meta).

## 6. Estrutura de arquivos principais

```
app/
  layout.js                  → RootLayout, importa fontes Inter (@fontsource) + globals.css.
                                Exporta `metadata` (title/description/manifest/icons/appleWebApp)
                                e `viewport` (width=device-width, initialScale=1, maximumScale=1,
                                userScalable=false, viewportFit=cover, themeColor) — ver seção 8 (PWA).
  page.js                    → redireciona pra rota certa conforme profile.role
  login/page.js
  colaborador/page.js        → busca profile, monta <AppShell><ColaboradorView/></AppShell>
  gerente/page.js            → idem, com <GerenteView/>
  socio/page.js               → idem, com <HierarchyHome role="socio"/>
  supervisor/page.js          → idem, com <HierarchyHome role="supervisor"/>
  admin/page.js                → PÁGINA GIGANTE (~2100 linhas). Início/Financeiro/Dados,
                                  CRUD de empresas/lojas/usuários, view-as universal,
                                  hierarquia de sócios/supervisores. Ver seção 9.
  api/
    account/update-username/route.js
    admin/
      create-empresa, create-loja, create-employee, create-gerente,
      create-hierarchy, update-employee, delete-employee, delete-empresa,
      hierarchy-access  (route.js em cada pasta)
  globals.css                 → base Tailwind + utilitário .scrollbar-hide + overflow-x:hidden defensivo
                                 + overscroll-behavior-y:none, -webkit-tap-highlight-color:transparent,
                                 touch-action:manipulation em html/body (sensação de app nativo — ver seção 8)
public/
  manifest.json                → PWA manifest (name, display:"standalone", theme_color #7c3aed,
                                  background_color #f5f3ee, ícones any + maskable)
  icon-192.png / icon-512.png / icon-maskable-512.png / apple-touch-icon.png / favicon-32.png / favicon-16.png
                                → gerados a partir do monograma de lib/Logo.js (círculos + seta, gradiente
                                  purple→pink) sobre fundo arredondado (any) e fundo full-bleed (maskable)

lib/
  AppShell.js                 → header global (logo, avatar/nome, sair, nav de abas). Usado por TODAS as telas.
  EditProfile.js               → dropdown "Meu perfil" (nome, username, foto de perfil, trocar senha)
  ChangePassword.js
  ColaboradorView.js
  GerenteView.js
  HierarchyHome.js             → sócio/supervisor + impersonation do master
  EmpresaDashboard.js          → dashboard completo de uma loja (~1650 linhas), reaproveitado por
                                  gerente/supervisor/sócio/master admin
  DateNav.js / MonthNav.js     → navegação de dia/mês
  ProgressBar.js / Led.js
  scoring.js                   → calcIndividualPct, calcTeamPct, currentGoalTarget, formatBRL, formatPct, motivationalMessage
  date.js                      → todayStr, firstDayOfMonth, remainingDaysInMonth, monthLabel, greeting, yesterdayStr
  MaskedInputs.js               → CurrencyInput, CnpjInput, PhoneInput
  Logo.js / Confetti.js
  supabaseClient.js             → client anon (browser)
  supabaseAdmin.js               → client service-role (só usado dentro de app/api/*)
```

## 7. Regras operacionais fixas (NÃO desviar)

1. **Nunca rodar `git push`.** O Claude só verifica que o build passa e entrega ao Felipe um comando pronto pra colar: `git add -A && git commit -m "..." && git push`.
2. **Fluxo de verificação de build:**
   ```bash
   rsync -a --delete --exclude node_modules --exclude .next --exclude .git "<pasta do projeto>/" ~/zmeta_build/
   cd ~/zmeta_build && npm run build
   ```
   Procurar por `✓ Compiled successfully`. Erros de prerender do tipo `Error: supabaseUrl is required` na fase de "Generating static pages" são **esperados** nesse ambiente de sandbox (não existe `.env.local` ali) — não indicam regressão, desde que a compilação em si tenha sido bem-sucedida.
3. **Se `package.json` mudar** (nova dependência): rodar `npm install` dentro de `~/zmeta_build` (por último, depois de já ter copiado os arquivos do repo pra lá) e então copiar o `package-lock.json` resultante de volta pro repositório real — senão o lockfile fica dessincronizado e quebra `npm ci` em produção. Cuidado com a ordem: um `rsync` do repo pra a pasta de build DEPOIS de rodar `npm install` sobrescreve o lockfile atualizado com o antigo — sempre `npm install` por último, e só então copiar o lockfile de volta.
4. **Depois de qualquer mudança de schema/RLS/storage no Supabase**, rodar `get_advisors(type:"security")` e confirmar que não surgiu nenhum alerta novo. O conjunto de avisos abaixo é **esperado e benigno** (não mexer):
   - `public_bucket_allows_listing` nos buckets `empresa-logos` e `avatars` (buckets públicos por design).
   - `anon_security_definer_function_executable` / `authenticated_security_definer_function_executable` em todas as funções `admin_*`, `is_*`, `can_*`, `my_*`, `get_team_progress`, `get_store_sales_ranking`, `prevent_gerente_edit_monthly_prize` — todas são `SECURITY DEFINER` mas verificam a role do caller internamente (`is_master_admin()` etc.) antes de fazer qualquer coisa, então o alerta do linter é um falso positivo conhecido.
   - `auth_leaked_password_protection` desabilitado.
5. **Master admin tem bypass de RLS** (`is_master_admin()`) em `empresas`, `loja_access`, `profiles`, `sales_entries`, `employee_prizes` — por isso várias ações do master em `app/admin/page.js` chamam o Supabase client diretamente (sem passar por `/api/admin/*`).
6. **Padrão de RLS usado nas tabelas operacionais** (tasks, warnings, sales_goals, sales_goal_allocations, sales_entries, app_settings, task_completions, employee_prizes):
   ```sql
   -- SELECT
   is_master_admin() OR employee_id = auth.uid() OR (is_gerente() AND loja_id = my_loja_id()) OR can_view_loja(loja_id)
   -- WRITE (insert/update/delete)
   is_master_admin() OR (is_gerente() AND loja_id = my_loja_id()) OR can_manage_loja(loja_id)
   ```
   Seguir esse padrão exato ao criar qualquer tabela nova ligada a colaborador/loja.
7. **Inputs mascarados** (`lib/MaskedInputs.js`): `<CurrencyInput value={number|""} onChange={...} />` pode ter `value === 0` (falsy em JS) — nunca validar com `if (!value)`, sempre `if (value === "" || value === null || value === undefined)`. Já foi um bug real (bloqueava lançar venda zerada).

## 8. Padrões de mobile-first estabelecidos (seguir em qualquer tela nova)

Auditoria mobile completa foi feita em todo o app numa sessão recente. Convenções fixadas:

- **Grids CSS com números/valores dinâmicos** (herocards de estatística) precisam de `min-w-0` no filho do grid + `break-words` no texto do valor + fonte reduzida em mobile (`text-lg sm:text-xl` ou `text-xl sm:text-3xl`, dependendo do contexto) — grid tracks não encolhem abaixo do conteúdo por padrão, diferente de flex.
- **Linhas flex com texto dinâmico + botões de ação** (nome de pessoa + badges + editar/excluir) levam `flex-wrap gap-2` como padrão de segurança, mesmo que `flex-shrink` já ajude na maioria dos casos.
- **Tabelas** (`<table>`) sempre dentro de `<div className="card overflow-x-auto">`.
- **Dropdowns/popovers posicionados com `absolute right-0`** levam `max-w-[calc(100vw-1.5rem)]` pra nunca vazar da viewport em telas de 320-360px.
- **Nav de abas do `AppShell`** usa `overflow-x-auto scrollbar-hide` com cada botão de aba em `shrink-0 whitespace-nowrap` — importante porque sócio chega a ter 5 abas (Início/Metas/Rankings/Faturamento/Supervisores).
- **O círculo de avatar/iniciais do header nunca deve ficar `hidden` em telas pequenas** — é o único gatilho visível pra abrir "Meu perfil" (bug real já corrigido uma vez, não reintroduzir).
- Utilitário `.scrollbar-hide` e `overflow-x: hidden` defensivo no `html, body` vivem em `app/globals.css`.
- **PWA / sensação de app nativo (fixado nesta sessão):** antes desta correção o app não tinha `public/manifest.json`, nenhum ícone e nenhuma meta viewport explícita — por isso o PWA instalado abria com zoom errado e com a barra de navegação do navegador visível (Android/iOS tratavam o "Adicionar à tela de início" como um bookmark comum, não como app instalável). Corrigido com: `export const viewport` em `app/layout.js` (width=device-width, initialScale=1, maximumScale=1, userScalable=false, viewportFit=cover), `manifest.json` com `display: "standalone"`, `appleWebApp: { capable: true, statusBarStyle: "black-translucent" }` no `metadata`, e `overscroll-behavior-y: none` + `-webkit-tap-highlight-color: transparent` + `touch-action: manipulation` em `html, body` no `globals.css` (elimina o "bounce" de scroll de navegador e o flash cinza de toque). **Importante: PWAs já instalados no celular do Felipe precisam ser removidos da tela de início e reinstalados (`Adicionar à Tela de Início` de novo) depois do deploy** — o modo de exibição (`standalone`) e os ícones são capturados no momento da instalação, não são aplicados retroativamente a um ícone já instalado.

## 9. Banco de dados — visão geral

**Tabelas principais:** `profiles` (id, full_name, role, active, username, empresa_id, loja_id, gerente_id, must_change_password, avatar_url), `empresas` (id, name, cnpj, telefone, email, logo_url, active, plano, valor_por_usuario, desconto, created_at), `lojas`, `sales_goals` (metas em camadas, por loja/mês), `sales_goal_allocations` (meta individual por colaborador), `sales_entries` (**coluna `daily_amount`** — valor vendido NAQUELE dia, não acumulado), `employee_prizes` (premiação livre por colaborador/mês — pode haver várias por mês), `warnings`, `tasks` (colunas `recurrence_type` `daily`/`weekly`/`once`, `weekday` 0-6 quando `weekly`, `once_date` quando `once` — ver seção 11 "Recorrência de tarefas"), `task_completions` (**FK `task_id` é `ON DELETE CASCADE`** — nunca fazer hard delete de uma `task` que já tem completions; ver seção 11), `commission_settings`, `app_settings`, `loja_access`.

> Nota histórica: a tabela `employee_stage_prizes` (premiação "por estágio do mês") e o conceito de "estágios" (dias 1-10/11-20/21-fim) que apareciam em rascunhos antigos deste documento **foram removidos do sistema**. O modelo atual de premiação é `employee_prizes`, simples, sem noção de estágio, lançado livremente pelo supervisor/master_admin.

**RPCs principais:** `admin_overview()`, `admin_lojas_health(p_month)`, `admin_financeiro(p_month)`, `admin_faturamento_mensal(p_empresa_id)`, `admin_delete_empresa(p_empresa)`, `get_team_progress(p_month, p_loja)`, `get_store_sales_ranking(p_month, p_loja)` (ranking de vendas da loja, ver seção 11), `is_master_admin()` / `is_socio()` / `is_supervisor()` / `is_gerente()`, `can_view_loja()` / `can_manage_loja()`, `is_my_team_member()`.

**Storage buckets:**
- `empresa-logos` — público, só master_admin escreve.
- `avatars` — público (leitura), cada usuário só escreve/edita/apaga dentro da própria pasta (`{uid}/avatar.ext`), master_admin com bypass.

### Técnica de simulação de RLS (útil pra debugar "por que esse usuário não vê X")
```sql
set local role authenticated;
select set_config('request.jwt.claims', json_build_object('sub','<uuid-do-usuario>','role','authenticated')::text, true);
<query a testar>
```
Rodar cada verificação como uma chamada separada de `execute_sql` — múltiplos statements com `;` só retornam confiavelmente o último result set.

## 10. Design tokens

Navy `#12203a` (texto principal), gold `#c9a15a`/goldlight `#e4c789`, purple `#7c3aed`, pink `#ec4899` (gradiente principal purple→pink em botões/gradient-text), verde (gerente) `#16a34a`/`#4ade80`, lilás (colaborador) `#a78bfa`/`#ddd6fe`, prata (sócio) `#94a3b8`/`#cbd5e1`, azul (supervisor) `#2563eb`/`#60a5fa`, teal `#0d9488`/`#5eead4`.

**Bug de CSS recorrente já corrigido na raiz:** classes customizadas (`.input`, `.btn`, `.btn-outline`, `.card`, `.label`, `.badge`) em `app/globals.css` precisam estar dentro de `@layer components { ... }`. Fora disso, `.label` (que tem `display:block`) vence utilities `flex`/`inline-flex` aplicadas junto na mesma className, quebrando ícone+texto em duas linhas. Se esse bug reaparecer, é porque uma classe nova foi adicionada fora do `@layer components`.

## 11. Histórico completo de features já construídas

### Fundação e comissionamento
- Comissão em camadas (colaborador/gerente + taxa de "não atingimento"), metas como níveis não-cumulativos (correção crítica, ver seção 5).
- RLS revisado e restrito por papel em várias rodadas; padrão fixado na seção 7.6.

### Colaborador
- Herocard com meta do dia, falta pra meta do mês, dias restantes no mês, atividades pendentes, comissão até agora, premiações.
- Checklist de tarefas com navegação por dia (`DateNav`), lançamento de venda (data sempre hoje, exceto correções feitas por gestor), aceita valor zero.
- Dashboards de metas do mês com barra de progresso por nível + "falta pra bater" por meta.
- Modal de parabéns/confete ao completar 100% das tarefas do dia (sem streak/foguinho — feature removida a pedido do Felipe).

### Gerente
- Herocard agregado da equipe (não da loja inteira — `gerente_id` escopa "minha equipe", já que uma loja pode ter vários gerentes com equipes diferentes).
- Pode ver como qualquer colaborador da própria equipe.
- Dashboard "Metas da loja" (read-only) e "Comissionamentos".
- Painel pessoal (tarefas/advertências/premiações atribuídas a ele mesmo pelo supervisor).

### Supervisor / Sócio (`HierarchyHome`)
- Sócio vê automaticamente todas as lojas da empresa (sem precisar de `loja_access`); supervisor só as lojas com acesso explícito.
- Seletor de loja + seletor de mês no topo.
- Abas: Início, Metas, Rankings (top vendedores / mais premiados / melhor barra / mais comissionados), Faturamento (por loja), e — só pra sócio — Supervisores (cadastro e gestão de acesso).
- Pode ver como qualquer gerente/colaborador das lojas sob gestão.

### Master Admin (`app/admin/page.js`)
- Abas: **Início** (visão geral: empresas ativas, usuários cadastrados, novas empresas, empresas esquecidas + gráfico de crescimento + form colapsável "Nova empresa" + lista de empresas com busca/ordenação), **Financeiro** (cobrança por empresa: usuários cadastrados × valor por usuário − desconto = quanto cobrar, editável), **Dados** (faturamento/usuários/premiações/lojas por empresa, com drill-down pra histórico mensal completo de faturamento por período, com presets 3/6/12 meses/tudo).
- **Ver como qualquer usuário** de qualquer nível hierárquico (colaborador/gerente via componentes existentes; sócio/supervisor via `HierarchyHome` com `impersonate`).
- Dashboard "Sócios e Supervisores" por empresa: vincular/desvincular lojas, ver colaboradores sob gestão, editar/resetar senha.
- Upload de logo por empresa (`EmpresaAvatar`).

### Perfil e conta
- **Foto de perfil pra todos os níveis** (bucket `avatars`, upload no dropdown "Meu perfil", exibida no círculo do header).
- Fonte do app trocada pra **Inter** (self-hosted via `@fontsource/inter`).

### Varredura geral de bugs (sweep completo)
Auditoria de todas as páginas/papéis/rotas de API/RLS já foi feita. Dois bugs reais foram encontrados e corrigidos (ambos em `HierarchyHome.js`, ligados à feature de impersonation do master): vazamento de identidade no header durante impersonation (ver seção 4), e aba Supervisores quebrada quando acessada via impersonation (faltava `empresaId` no payload da API).

### Otimização mobile (mais recente)
Auditoria completa de responsividade em todo o app — ver seção 8 pros padrões fixados. Bugs reais corrigidos: avatar do header invisível em mobile (`hidden sm:flex` no gatilho de "Meu perfil"), nav de abas sem scroll horizontal, herocards com risco de overflow em telas estreitas (grids CSS sem `min-w-0`), dropdowns sem clamp de largura (`w-80`/`w-56` vazando em telas de 320px).

### PWA — manifest, ícones e viewport (sessão mais recente)
O app nunca teve `public/manifest.json` nem ícones — o "PWA" instalado por Felipe era, na prática, um bookmark do navegador. Dois bugs reais corrigidos, ver seção 8 pro detalhe técnico completo:
1. **Zoom incorreto ao abrir** — não havia `export const viewport` em `app/layout.js`. Adicionado com `initialScale: 1, maximumScale: 1, userScalable: false, viewportFit: "cover"`.
2. **Barra de navegador visível em vez de tela cheia** — sem manifest com `display: "standalone"` e sem `appleWebApp` no `metadata`, tanto Android quanto iOS tratam "Adicionar à Tela de Início" como atalho comum. Criado `public/manifest.json` (ícones 192/512/maskable-512 gerados a partir do monograma de `lib/Logo.js`) + `apple-touch-icon.png` + `manifest`/`icons`/`appleWebApp` em `metadata`.
Bônus de sensação nativa: `overscroll-behavior-y: none`, `-webkit-tap-highlight-color: transparent`, `touch-action: manipulation` em `html, body` (`globals.css`) — elimina bounce de scroll estilo navegador e o flash cinza ao tocar em botões.
**Pendência do lado do Felipe:** reinstalar o PWA (remover da tela de início + "Adicionar à Tela de Início" de novo) depois do deploy, já que ícone/modo de exibição não atualizam num atalho já instalado.

### Ranking de vendas na visão do colaborador (nova feature)
Adicionado no `tab === "atividades"` (tela inicial) do `ColaboradorView.js`:
- Card **"Ranking de vendas"** logo abaixo de "Minhas metas" — lista todos os colaboradores ativos da mesma loja ordenados por valor vendido no mês, com a linha do próprio usuário destacada (fundo roxo claro, "(você)").
- No herocard, novo stat **"Posição no ranking"** ao lado de "Premiações" (grid do herocard passou de `sm:grid-cols-5` pra `sm:grid-cols-3 lg:grid-cols-6` pra caber o 6º item).

**Detalhe técnico importante (RLS):** a política de `profiles_select` só libera ver o perfil de outra pessoa via `can_view_loja()`, que só retorna true pra gerente/sócio/supervisor/master_admin — um colaborador comum NUNCA teria acesso ao nome/perfil de um colega pela RLS direta. Por isso o ranking não é montado com queries client-side em `profiles`/`sales_entries` (retornaria só o próprio usuário); foi criada a function `public.get_store_sales_ranking(p_month date, p_loja uuid default null)` no Supabase — `SECURITY DEFINER`, mesmo padrão de `admin_financeiro`/`get_team_progress`: usa `p_loja` se informado (default `my_loja_id()`), confere internamente `is_master_admin() OR v_loja = my_loja_id() OR can_view_loja(v_loja)` antes de retornar qualquer linha, e devolve `employee_id, full_name, sold` só dos colaboradores ativos daquela loja. Testada simulando RLS como colaborador de verdade (retornou os 4 colegas da loja corretamente) e via `get_advisors(security)` (só apareceu o alerta esperado de "SECURITY DEFINER executável" — mesma categoria benigna de sempre, ver seção 7.4; adicionar `get_store_sales_ranking` à lista de funções conhecidas ali). Client-side, `ColaboradorView.js` chama `supabase.rpc("get_store_sales_ranking", { p_month: monthArg, p_loja: lojaId })` dentro do `loadAll`.
**Padrão a repetir:** qualquer feature futura que precise "colaborador vendo dado agregado de colegas" deve passar por uma function `SECURITY DEFINER` assim — nunca tentar resolver abrindo a RLS de `profiles`/tabelas operacionais pra colaborador comum (quebraria o isolamento entre colaboradores, que é intencional).

### Rodada de ajustes visuais/mobile (sessão mais recente)
1. **Herocard do gerente — líder de vendas.** `GerenteView.js`: `loadStats` agora calcula, a partir do `entryRows` da equipe (já buscado ali mesmo), quem vendeu mais no mês (`leaderName`), e o herocard ganhou um 6º stat "Líder de vendas" ao lado de Premiações (mesmo tratamento de grid `sm:grid-cols-3 lg:grid-cols-6` já usado no herocard do colaborador).
2. **Abas Placar/Colaboradores/Tarefas/Advertências/Premiações quebrando em 2 linhas no mobile.** Eram o componente `SubNav` (`EmpresaDashboard.js`) — usado por Início E Metas, em todas as visões (gerente/supervisor/sócio/master, já que é reaproveitado por `EmpresaDashboard`) — com `flex flex-wrap`, por isso quebrava em vez de rolar. Trocado por `flex-1` em cada botão no mobile (soma sempre a largura do container, nunca estoura nem quebra linha), ícone em cima + label truncado embaixo, fonte bem pequena (`text-[9px]`); a partir de `sm:` volta ao pill horizontal original (ícone+label lado a lado, fonte maior). **Não** mexemos nas abas de nível superior do `AppShell` (Início/Metas/Rankings/Faturamento/Supervisores do sócio) — essas continuam com scroll horizontal (padrão já documentado na seção 8) porque não foi isso que foi reportado como quebrado.
3. **Texto "Se o mês fechasse hoje" mostrando o valor do prêmio.** Só existia em `EmpresaDashboard.js` (`Placar`, card "Barra geral da equipe", visível pra gerente e master_admin — supervisor/sócio veem uma barra diferente, "Barra de vendas", que nunca mostrou valor). Trocado pra `Se o mês fechasse hoje: premiação liberada!` / `... premiação zerada (abaixo de X%).` — sem valor em nenhum dos dois casos. `ColaboradorView.js` já não mostrava o valor (mensagem "Prêmio garantido se seguir assim!" / "Prêmio do mês liberado com X%+..."), não precisou mexer.
4. **Títulos (`<h1>`) grandes demais no mobile, em todas as páginas/visões.** Todos os `<h1>` do app (`ColaboradorView`, `GerenteView`, `HierarchyHome`, `app/admin/page.js` — Início, loja, Financeiro, Dados, empresa, sócios/supervisores) foram trocados de tamanho fixo pra responsivo: `text-xl font-bold` → `text-lg sm:text-xl font-bold`, e o único `text-2xl font-extrabold` (saudação do colaborador) → `text-xl sm:text-2xl font-extrabold`. Desktop fica idêntico, só o mobile encolhe.
5. **Placar individual.** Removido o subtítulo "— ranqueado por valor vendido" (ficou só "Placar individual"). No mobile, a tabela larga (Colaborador/Tarefas/Advertências/Vendido/%) foi substituída por uma lista de blocos por colaborador — nome + valor vendido na mesma linha, barra de progresso **abaixo** do nome, tarefas/advertências como legenda pequena embaixo. A partir de `sm:` continua a tabela de sempre (`hidden sm:table` / `sm:hidden` fazem a troca).
6. **Ordem "Distribuição da meta em jogo" vs "Configurações".** Só existia essa ordem invertida na visão do **gerente** (`GerenteView.js`): o card "Distribuição da meta em jogo" (leitura da distribuição da meta atual entre a equipe) vivia DEPOIS do `<EmpresaDashboard>` inteiro — ou seja, abaixo até do card "Configurações" (prêmio/desconto/meta da barra) que mora dentro do Placar. Movido pra cima do `<EmpresaDashboard>`, logo após o herocard — agora aparece antes de qualquer coisa vinda do EmpresaDashboard, incluindo Configurações. "Painel pessoal" continua na mesma posição de antes (depois do EmpresaDashboard).
7b. **Subtítulos removidos/simplificados na aba Metas (`EmpresaDashboard.js`, todas as visões).** Card "Comissionamentos": removida a frase explicativa abaixo do título ("Taxa aplicada enquanto ninguém bateu..."). Card de cada meta: o subtítulo (que tinha valor da meta + % comissão colaborador + % comissão gerente + modo de distribuição) virou só o valor da meta (`{formatBRL(g.store_total)}`) — o resto dessa informação (comissão, distribuição) continua visível mais abaixo no próprio card, só não repete no subtítulo.
7c. **Auditoria do cálculo de "atividades pendentes" (herocard gerente) vs. "Placar individual" (Tarefas completed/expected).** Felipe estranhou ver "4 pendentes" no herocard do gerente enquanto o Placar individual mostrava "50/55" pro colaborador Gustavo. Verificado direto no banco (Gustavo tem 4 tarefas ativas; nenhuma feita hoje ainda; os outros 3 da equipe do gerente não têm tarefa nenhuma cadastrada) — **os dois números batem e ambos os cálculos estão certos**: "atividades pendentes" no herocard é só de HOJE, somando o time inteiro (4 tarefas ativas, 0 feitas hoje = 4 pendentes); "50/55" no Placar é acumulado do MÊS INTEIRO, só do Gustavo (tarefas esperadas x concluídas dia a dia desde o dia 1). São métricas de janelas de tempo e escopos diferentes — não deveriam bater, e a comparação direta que gerou a estranheza não é um bug.
   Durante essa auditoria achei um bug real e diferente, já corrigido: o `board`/placar (`EmpresaDashboard.js`, função `loadAll`) montava o placar a partir de `emps` **sem filtrar `active`** — diferente da query de "atividades pendentes" do `GerenteView.js`, que já filtrava só ativos. Ou seja, um colaborador desligado continuaria aparecendo no Placar individual/ranking/barra da equipe (com números desatualizados), mesmo já tendo sumido do "atividades pendentes" do herocard. Corrigido adicionando `.filter((emp) => emp.active)` antes de montar o `board` — a aba Colaboradores continua listando todo mundo (inclusive inativos, pra dar pra reativar); só o placar/ranking passou a considerar apenas ativos.

### Exclusão de tarefa sem apagar histórico + recorrência de tarefas (sessão mais recente)
Dois pedidos do Felipe na mesma sessão, ambos em torno de `tasks`/`task_completions`:

1. **"Excluir" tarefa não pode apagar dias já realizados.** Causa raiz: a FK `task_completions.task_id → tasks.id` é `ON DELETE CASCADE`. O botão "excluir" de `Tarefas` (`EmpresaDashboard.js`) fazia `DELETE FROM tasks`, que em cascata apagava **todo** o histórico de completions daquela tarefa — inclusive dias já concluídos, reescrevendo indicadores passados (mês fechado, barra da equipe, comissão etc. de meses anteriores mudariam retroativamente). Corrigido: `removeTask` agora nunca faz `DELETE` na tabela `tasks`. Em vez disso: (a) apaga só as linhas de `task_completions` dessa tarefa que sejam `completed = false` **e** `completion_date >= hoje` (ou seja, só pendências de hoje em diante — dias passados, feitos ou perdidos, nunca são tocados); (b) marca a tarefa como `active = false` (mesmo efeito de "pausar", só que iniciado pelo botão "excluir", com confirmação via `window.confirm`). Resultado prático: a tarefa para de valer dali pra frente, mas nenhum dia já registrado é apagado ou alterado — nem os feitos, nem os perdidos.
   **Padrão a repetir:** qualquer ação de "excluir" numa tabela com histórico dependente (FK `ON DELETE CASCADE` ou não) deve ser auditada com a mesma pergunta — "isso apaga fatos já acontecidos?" — antes de implementar como hard delete.

2. **Recorrência de tarefas (diária / 1x por semana / só uma vez).** Antes, toda tarefa cadastrada era implicitamente "todo dia, pra sempre" — não existia conceito de recorrência no schema. Adicionado:
   - **Migração** (`add_task_recurrence`): `tasks` ganhou `recurrence_type` (`'daily'` | `'weekly'` | `'once'`, default `'daily'` — todas as tarefas antigas continuam válidas como diárias, sem precisar migrar dado nenhum), `weekday` (0-6, domingo=0, só quando `weekly`) e `once_date` (só quando `once`). Constraint `tasks_recurrence_shape_chk` garante que cada tipo só preenche o campo que faz sentido pra ele (ex.: não dá pra ter `recurrence_type='daily'` com `once_date` preenchido).
   - **Helper novo** `isTaskDueOn(task, dateStr)` em `lib/date.js` (junto de `WEEKDAY_LABELS`) — decide se uma tarefa "vale" num dia específico. É o único lugar que sabe a regra; todo o resto do app só chama essa function.
   - **Onde a regra é aplicada:** o pulo do gato é que a regra só precisa ser checada no **momento de semear** a linha de `task_completions` do dia (e ao decidir o que mostrar no checklist daquele dia) — os agregados do mês (`expected`/`completed`, barra individual, ranking) continuam contando linhas que já existem em `task_completions`, sem precisar de nenhuma mudança, porque uma tarefa semanal/única só vai ter linha nos dias em que realmente esteve em jogo. Ajustado em três lugares:
     - `ColaboradorView.js` (`loadAll`): só semeia hoje e só conta como "hoje" as tarefas com `isTaskDueOn(t, hoje)`; o checklist (`viewTasksList`) e o card "Tarefas de hoje" (herocard: `doneCount`/`pendingCount`/`todayPct`, e o gatilho do modal de parabéns) passaram a usar esses conjuntos filtrados por dia, tanto pra hoje quanto pro dia sendo navegado via `DateNav`.
     - `GerenteView.js` (`loadStats`): "Atividades pendentes" do herocard passou a filtrar por `isTaskDueOn` antes de contar (precisou trocar o `select("id")` por `select("id, recurrence_type, weekday, once_date")`); o "Painel pessoal" (tarefas atribuídas diretamente ao gerente) também só mostra/conta as tarefas do dia.
     - `EmpresaDashboard.js` (`Tarefas`): formulário de criação ganhou os 3 botões de recorrência ("Todos os dias" / "1 dia na semana" / "Só uma vez") com seletor de dia da semana ou data conforme a escolha (inclusive no modo "replicar pra todos os colaboradores da loja" — todos recebem a mesma regra); a lista de tarefas cadastradas mostra a recorrência de cada uma (`recurrenceLabel`: "todo dia" / "toda [dia]" / "só em [data]"); o "Checklist do dia" (`dueTasks`) só mostra quem vale no dia sendo visualizado.
   **Testado** no Supabase (insert de uma tarefa de cada tipo dentro de uma transação com rollback, conferindo os 3 formatos salvos certos) e `get_advisors(security)` sem novidade além do padrão já conhecido de SECURITY DEFINER.

### Aba Metas ainda poluída pra supervisor/sócio (sessão mais recente)
As limpezas anteriores (subtítulos, fonte menor) já valiam pra todo mundo por ser componente compartilhado, mas quem tem `canManage` (supervisor/sócio/master_admin) vê MUITO mais UI que o gerente (que só visualiza) — formulário de criar meta sempre aberto, e a lista de distribuição de cada meta listando **todo colaborador individualmente, um por um**, mesmo quando o valor é igual pra todos. Isso é provavelmente a real fonte da "poluição" que o gerente não sofre tanto. Duas mudanças em `EmpresaDashboard.js` (`Metas`):
1. **"Nova meta" virou colapsável** (mesmo padrão já usado em "Nova empresa" no admin e "Novo gerente"/"Nova empresa": botão com chevron, formulário fecha por padrão e depois de criar a meta). Reduz um formulário de 5 campos sempre visível pra um botão de uma linha.
2. **Distribuição da meta: lista vira resumo de uma linha quando é igual pra todos.** Antes, cada card de meta sempre listava nome+valor de cada colaborador (repetindo o mesmo valor várias vezes quando a distribuição é igual — o caso mais comum). Agora, se `isEvenSplit(goalAllocs)` for verdadeiro, mostra só "`N colaborador(es) · R$X cada`"; a lista pessoa-a-pessoa só aparece quando a distribuição é de fato custom (valores diferentes por pessoa, onde listar faz sentido).
**Ponto cego encontrado e corrigido: modo leitura para `viewerRole === "leitor"` em toda a `EmpresaDashboard.js`.** Confirmado pelo Felipe ("Sim, corrige agora" → depois "Corrige tudo agora (recomendado)" ao ver que o escopo era maior do que parecia). Achado original: `Metas()`, `Placar()` e `Colaboradores()` usavam `viewerRole !== "gerente"` pra liberar edição, o que incluía sem querer o viewerRole `"leitor"` (supervisor/sócio com permissão só de "ver", não "gerenciar", numa loja via `loja_access` — setado em `HierarchyHome.js` como `viewerRole={canManageSelected ? role : "leitor"}`). Investigando mais a fundo, o problema era ainda maior: `Tarefas()`, `Advertencias()` e `Premiacoes()` não tinham NENHUM gate — qualquer viewerRole conseguia criar/editar/excluir tarefas, advertências e premiações, e marcar o checklist de qualquer colaborador, mesmo sendo só "leitor".
  - **Correção aplicada** (padrão repetido em cada componente): variável `canEdit`/`canManage`/`canManageTeams`/`canEditPrize`/`canEditSettings` excluindo `"leitor"` além de `"gerente"` onde já se aplicava, escondendo formulários de criação e botões de ação (editar/excluir/pausar/reativar) quando o viewer é só leitor. No checklist de tarefas (`Tarefas()`), o botão de marcar/desmarcar vira um ícone não-clicável pra leitor. `Metas()`: `canManage` passou de `viewerRole !== "gerente"` para `viewerRole !== "gerente" && viewerRole !== "leitor"`. `Lancamentos()` não recebia `viewerRole` como prop — adicionado (o call-site já passava, só o componente não usava) e o formulário "Corrigir/lançar valor vendido" agora é escondido pra leitor.
  - **Nuance de severidade importante:** isso era um bug só de UI, não um buraco de segurança de dado. A função `can_manage_loja(p_loja)` do Postgres — que já governa TODAS as policies RLS de escrita no banco (tasks, warnings, employee_prizes, goals, sales_entries, etc.) — exige `loja_access.permission = 'gerenciar'`. Um "leitor" (permission = 'ver') sempre falharia essas escritas no banco, mesmo antes dessa correção. Ou seja, o pior cenário real era um leitor ver botões que, ao clicar, dariam erro silencioso ou visível do Supabase — não um vazamento ou alteração de dado indevida. Essa correção resolve a experiência confusa/quebrada, não uma vulnerabilidade de dados.
  - **Fora do escopo desta correção, sinalizado mas não implementado:** o fluxo "ver como" (impersonar um colaborador/gerente a partir da visão de supervisor/sócio) não tem modo leitura de verdade — um "leitor" usando "ver como" ainda consegue, pela UI de `ColaboradorView`/`GerenteView`, tentar marcar tarefas ou lançar vendas como se fosse aquela pessoa (também bloqueado no banco pela mesma RLS `can_manage_loja`, então mesmo nuance de severidade se aplica). Corrigir isso de verdade exigiria passar um flag de somente-leitura por essas duas views inteiras — mudança arquitetural maior, não pedida ainda.
7. **Textos menores na aba Metas, em todas as visões.** Em `EmpresaDashboard.js` (`Metas`/`Lancamentos`, usados por gerente/supervisor/sócio/master) e em `ColaboradorView.js` (aba Metas do colaborador): títulos de cada card de meta, listas de comissionamento/distribuição e o histórico de lançamentos ganharam `text-xs sm:text-sm` (ou `text-[11px] sm:text-xs`) no lugar de `text-sm`/`text-xs` fixo — só encolhe no mobile, desktop sem mudança. Os números grandes de destaque (Meta de hoje, Falta pra bater, valores do herocard) foram mantidos do tamanho que já estavam — o pedido era limpar o texto "miúdo", não os números-destaque.

**Atualização — confirmado por Felipe:** funcionou perfeitamente no iOS. No Android, o PWA instalado **não rolava (scroll) pra cima nem pra baixo, travado por completo**. Causa: `overscroll-behavior-y: none` + `touch-action: manipulation`, aplicados em `html, body` (globals.css) — bug conhecido do Chrome/WebView do Android, onde essas duas props no elemento raiz de scroll travam a rolagem inteira da página no contexto de PWA instalado (WebAPK). iOS não é afetado da mesma forma. **Removidas as duas** de `globals.css`; ficou só `-webkit-tap-highlight-color: transparent` (puramente cosmético, sem risco de travar scroll). **Lição: não reintroduzir `overscroll-behavior` nem `touch-action` no elemento raiz (`html`/`body`) sem testar em Android real antes** — teste só em iOS não é suficiente pra essas duas props.

**Atualização — o scroll continuou travado no Android mesmo depois disso.** Causa: sobrou um `overscroll-none` (utilitário Tailwind = `overscroll-behavior: none`) direto no `className` do `<body>` em `app/layout.js` — a limpeza anterior só tinha removido a declaração equivalente do `globals.css`, mas esse mesmo culpado voltava por um segundo caminho (classe no JSX) que passou despercebido. Removido. **Lição adicional: ao caçar esse tipo de bug, checar os DOIS lugares — CSS global E className inline nos componentes — não só um.**

### Dois bugs reais corrigidos numa sessão seguinte
1. **Financeiro (master) mostrava "Usuários cadastrados" zerado.** Causa: em `FinanceiroTab` (`app/admin/page.js`), o `useMemo` de `totals` calculava a variável `usuarios` mas **não a incluía no objeto retornado** (`return { receita, colaboradores, ticketEmpresa, ticketUsuario }` — faltava `usuarios`), então `totals.usuarios` chegava `undefined` no `HeroStat`. O RPC `admin_financeiro` em si sempre esteve correto (confirmado rodando a função direto no Supabase). Corrigido incluindo `usuarios` no retorno. A aba **Dados** nunca teve esse bug (já retornava `usuarios` certinho).
2. **Campo "Dia da venda" (card "Lançar valor vendido ontem", `ColaboradorView.js`) gigante e desproporcional no mobile.** Causa: era um `<input type="date">` nativo do HTML, que em alguns Android/iOS renderiza o valor bem maior que os outros inputs quando não focado. Numa primeira tentativa foi trocado pelo componente `DateNav` (setas anterior/seguinte). **Felipe pediu de volta o calendário nativo** (precisa poder escolher qualquer dia do mês, não só andar dia a dia com setas) — revertido pro `<input type="date">`, agora com a classe `date-input` e uma regra dedicada em `globals.css` (`input[type="date"].date-input { -webkit-appearance: none; appearance: none; font-size: 0.875rem; line-height: 1.25rem; color-scheme: light; }`) forçando o mesmo tamanho de fonte dos outros campos, na tentativa de evitar o "pill" gigante sem perder o calendário. `max={today}` no próprio input substitui o `maxDate` que o `DateNav` fazia. **Isso é best-effort — não foi possível testar em dispositivo Android/iOS real dentro desta sessão; se o campo voltar a ficar desproporcional, os próximos passos seriam ajustar essa regra CSS com mais força ou, em último caso, construir um calendário próprio (não nativo).** `DateNav` continua em uso normalmente na navegação de dia das Tarefas — não foi removido do projeto.

### Textos quebrando em 2 linhas de forma feia (política geral anti-quebra)
Felipe reportou que na aba **Rankings** (`RankingsTab`/`RankingCard` em `HierarchyHome.js`, visão supervisor/sócio — lista cruzada "Top vendedores"/"Mais premiados"/"Melhor barra"/"Mais comissionados" entre todas as lojas) as linhas de cada item quebravam em 2 linhas de forma feia no mobile, e pediu uma regra geral: **nenhum dashboard do app pode quebrar texto em 2 linhas de forma quebrada** — sempre diminuir a fonte antes de deixar quebrar, porque empresas futuras vão trabalhar com metas de valores maiores (mais dígitos = strings mais longas). Também pediu limite de caracteres nos nomes de usuário para evitar esse tipo de problema.
1. **`RankingCard` (`HierarchyHome.js`).** Cada linha tinha nome + "· loja" + valor todos soltos dentro de um `flex justify-between` sem `truncate`/`shrink-0` — nome+loja longos empurravam o valor pra quebrar linha. Corrigido: nome+loja viram um único `<span className="truncate">` (corta com "…" em vez de quebrar), valor ganha `shrink-0 whitespace-nowrap`, e a fonte do item cai pra `text-xs sm:text-sm`.
2. **Padrão repetido em todo lugar que lista "nome da meta + valor" ou "nome do colaborador + valor".** Mesmo bug de raiz existia em vários pontos: lista "Metas da loja" (dentro do herocard do supervisor/sócio em `HierarchyHome.js`), `Placar()` (`EmpresaDashboard.js`, cards "Metas da loja" e "Premiações lançadas"), `Metas()` (`EmpresaDashboard.js`, cards de cada meta — já usava `text-xs sm:text-sm` de uma limpeza anterior, mas sem `truncate`/`shrink-0`), e o ranking de vendas do colaborador (`ColaboradorView.js`). Em todos: nome do lado esquerdo ganhou `min-w-0` + `truncate` (ou span dedicado pra truncar), valor/badge do lado direito ganhou `shrink-0 whitespace-nowrap`. Os herocards com números grandes (Meta de hoje, Falta pra meta, etc., em `HierarchyHome.js`/`EmpresaDashboard.js`/`GerenteView.js`/`ColaboradorView.js`) já tinham esse cuidado de sessões anteriores (`min-w-0` + `break-words` dentro de cada célula do grid) — não precisaram de mudança.
3. **Limite de caracteres nos campos de nome/usuário**, pra reduzir a chance de nomes absurdamente longos causarem esse tipo de problema em primeiro lugar: `maxLength={40}` em todo input de "nome completo" (colaborador, gerente, supervisor/sócio, edição de nome de usuário existente) e `maxLength={20}` em todo input de "usuário (login)", tanto em `EmpresaDashboard.js` (`Colaboradores()`) quanto em `app/admin/page.js` (`AddGerenteForm`, `AddColaboradorForm`, `AddHierarchyForm`, edição de usuário existente). Também `maxLength={50}` no nome da empresa e `maxLength={30}` no nome da meta (`Metas()`), já que esses nomes aparecem lado a lado com valores nas mesmas linhas afetadas pelo item 2.
   **Nota importante:** esse `maxLength` é só client-side (trava só no formulário). As rotas de API (`/api/admin/create-employee`, `create-gerente`, `create-hierarchy`) que de fato gravam no banco não validam esse limite — então tecnicamente ainda é possível, batendo direto na API, gravar um nome mais longo. Não implementei validação/constraint no servidor ou no banco porque isso era um pedido de polimento visual, não de integridade de dado, e uma constraint de banco exigiria migração e cuidado com nomes já cadastrados que porventura já sejam mais longos. Se quiser fechar esse ponto de verdade (defesa em profundidade), a próxima etapa seria validar o tamanho também dentro das rotas de API.
   **Build verificado:** `✓ Compiled successfully`.

### Placar individual removido do Início; Ranking de vendas chega no gerente e no supervisor (sessão seguinte)
Felipe pediu três ajustes na visão do supervisor mobile: (1) o título "Barra de vendas — vendido / meta — {mês}" (branch `isSupervisorView` de `Placar()`, `EmpresaDashboard.js`) estava grande demais — virou só "Barra de vendas — {mês}" (removido "vendido / meta"); (2) excluir o card **"Placar individual"** (lista mobile + tabela desktop, dentro de `Placar()`) da aba Início, **pra todos os usuários** (gerente, supervisor, sócio, master admin — é um componente compartilhado); (3) em troca, levar o dashboard **"Ranking de vendas"** (que já existia só na Início do colaborador, mostrando a equipe ordenada por valor vendido) também pra visão do gerente e do supervisor.
1. **Placar individual removido.** O bloco inteiro (ambas as versões, mobile `<ul>` e desktop `<table>`) saiu de `Placar()` em `EmpresaDashboard.js`. A variável `ranked` (usada pra ordenar esse placar) continuou no código porque o card **"Líder de vendas até agora"**, logo acima, também depende dela (`ranked.find(...)` pra achar o líder) — não removida, só o card de baixo.
2. **Ranking de vendas no gerente (`GerenteView.js`).** Em vez de chamar a RPC `get_store_sales_ranking` (que existe pra contornar a RLS que bloqueia um colaborador comum de ver perfis dos colegas), o gerente **já tem** os dados prontos: `loadStats` já monta um `soldByEmp` (mapa employee→total vendido no mês) pra calcular o "Líder de vendas" do herocard. Só precisei reaproveitar esse mesmo mapa pra montar a lista ordenada (`storeRanking`) e renderizar o card, sem query nova. Importante: esse ranking é escopado à **própria equipe do gerente** (mesmo filtro `gerente_id = this.gerente.id` que já valia pro resto do dashboard do gerente), não a loja inteira — consistente com o resto dos números que ele já vê (meta, vendido, comissão são todos só da equipe dele, não da loja toda, já que uma loja pode ter mais de um gerente/equipe).
3. **Ranking de vendas no supervisor/sócio (`HierarchyHome.js`).** Mesma lógica: `loadHero` já busca `emps` (colaboradores ativos da loja) e `entryRows` (vendas do mês) pra calcular o herocard da loja inteira — só precisei trocar a query de `emps` pra também trazer `full_name` (antes só trazia `id`) e montar `soldByEmp`/`storeRanking` a partir do que já estava sendo buscado. Aqui o ranking é da **loja inteira** (todas as equipes juntas), consistente com o resto do herocard do supervisor que também agrega a loja toda. Card renderizado na visão por loja, logo depois de "Metas da loja" e antes do `<EmpresaDashboard>`.
   **Nota:** nenhuma dessas duas implementações usa a RPC `get_store_sales_ranking` — ela continua existindo e em uso só pelo colaborador (`ColaboradorView.js`), que é o único papel sem acesso direto via RLS aos perfis dos colegas. Gerente e supervisor/sócio já tinham `can_view_loja`/acesso direto, então bastou reaproveitar dado já carregado, mais rápido que uma call de RPC extra.
   **Build verificado:** `✓ Compiled successfully`.

### Leva de polimento visual: despoluir Placar/Colaboradores/Tarefas/Metas e reformular Rankings/Faturamento (sessão seguinte)
Lista grande de ajustes pedidos por Felipe na visão supervisor mobile e em componentes compartilhados. Regra que ele reforçou no fim: **dashboards compartilhados por vários tipos de usuário precisam ter a mesma cara — só muda O QUE cada um vê, não o layout** — por isso sempre que uma mudança valia pra um componente usado por mais de um papel, apliquei pra todos os papéis que passam por ali, e quando um componente é exclusivo de um papel, chequei se existe um equivalente noutro papel que precisasse do mesmo tratamento.
1. **`Placar()` (`EmpresaDashboard.js`) — dois cards saíram da visão supervisor/sócio:**
   - **"Configurações"** (desconto por advertência, meta da barra geral, premiação mensal) some inteiro quando `isSupervisorView` (supervisor ou sócio) — pedido explícito do Felipe só pra essa visão; continua existindo normalmente pra gerente (só leitura) e master admin (edição).
   - **"Líder de vendas até agora"** passou a só aparecer quando `viewerRole === "master_admin"`. Motivo: esse card virou redundante pra quem já tem o dashboard "Ranking de vendas" na tela de Início (gerente e supervisor/sócio, adicionados na sessão anterior; colaborador já tinha há mais tempo) — só o master admin, que nunca passa pelo `HierarchyHome`/`GerenteView` (vê a loja direto via `<EmpresaDashboard viewerRole="master_admin">` em `admin/page.js`), ainda depende desse resumo aqui dentro do Placar.
2. **`Colaboradores()` (`EmpresaDashboard.js`) — campo "liderados" ao criar gerente virou colapsável.** Label mudou de "Colaboradores dessa loja pra já incluir na equipe (opcional)" pra **"Definir liderados"**; a lista de pills com nome de cada colaborador (sempre visível, ocupando bastante espaço) virou um dropdown: um botão mostrando "N colaborador(es) selecionado(s)" com seta (chevron), que revela as pills só quando clicado (`gTeamOpen` state, mesmo padrão collapse já usado em "Nova meta"/"Nova empresa"). Fecha de novo automaticamente depois de criar o gerente.
3. **`Tarefas()` (`EmpresaDashboard.js`) — texto "Replicar essa tarefa para todos os colaboradores da loja" reduzido** de `text-xs` pra `text-[11px] sm:text-xs`, evitando a quebra feia em 2 linhas no mobile.
4. **`Metas()` (`EmpresaDashboard.js`) — dois ajustes:**
   - Título "Premiação mensal da loja — {mês}" virou só **"Premiação mensal"** (o mês já aparece em outros lugares da mesma tela, não precisava repetir aqui).
   - Botões de ação de cada card de meta ("editar valores" / "editar distribuição" / "excluir") **viraram ícone-only** (`Coins`/`Split`/`Trash2` do lucide-react, com `title`/`aria-label` pra acessibilidade, sem texto visível) — reduz a poluição visual de cada card, principalmente no mobile onde os três botões com texto competiam por espaço com o nome da meta.
5. **`RankingCard` (`HierarchyHome.js`, aba Rankings do supervisor/sócio) — layout de cada item virou empilhado verticalmente** em vez de espremido numa linha horizontal: nome, loja e valor agora ficam cada um na sua própria linha (nome em negrito, loja menor e discreta logo abaixo, valor em negrito por último), no lugar do layout anterior que juntava nome+loja truncados de um lado e valor do outro. Mais legível e ninguém mais compete por largura de linha, então não quebra mesmo se os valores crescerem.
6. **Card "Melhor barra individual" renomeado pra "Líder de tarefas concluídas"** (mesmo dado — % da barra individual de tarefas — só o nome ficou mais claro sobre o que representa).
7. **`FaturamentoTab` (`HierarchyHome.js`) — tabela "Faturamento por loja" corrigida.** Os cabeçalhos "Vendido no mês" e "% do total" quebravam em 2 linhas porque a tabela não tinha nenhuma proteção contra quebra (sem `whitespace-nowrap`, sem controle de fonte) — o navegador preferia quebrar o texto do cabeçalho a deixar a tabela estourar a largura do card. Corrigido: todas as células (cabeçalho e dados) ganharam `whitespace-nowrap`, e a fonte caiu pra `text-xs sm:text-sm` (cabeçalho `text-[10px] sm:text-xs`) — como o card pai já tem `overflow-x-auto`, agora se algum valor ficar grande demais (empresa com metas maiores, mais dígitos) a tabela rola horizontalmente em vez de quebrar texto feio.
   **Verificação de consistência:** o equivalente do master admin (lista de empresas na aba Dados, `admin/page.js`, com "Faturamento no mês"/"Premiações pagas" por empresa) usa um layout diferente — card em grid, não tabela — e já tinha `min-w-0` + `break-words` de uma limpeza anterior, então já era resistente a esse mesmo problema por outro caminho; não precisou de mudança.

### Porcentagem da barra em "Minhas metas" (sessão seguinte)
Felipe pediu pra mostrar a porcentagem da barra em cada meta no dashboard "Minhas metas" (visão colaborador). Causa: o `<ProgressBar>` de cada meta nessa lista tinha `showLabel={false}` (o componente `ProgressBar.js` só desenha o LED colorido + "{pct}%" abaixo da barra quando `showLabel` é `true`, que é o padrão). Removido o `showLabel={false}` em `ColaboradorView.js`. Pela regra de consistência entre dashboards compartilhados, apliquei a mesma mudança nos dois lugares que renderizam essa mesma lista de metas com barra (mesmo componente conceitual, só o alvo muda — individual vs. loja inteira): "Metas da loja" na aba Placar do gerente (`EmpresaDashboard.js`) e "Metas da loja" na visão do supervisor/sócio (`HierarchyHome.js`). Não mexi no card de metas individuais maior mais abaixo em `ColaboradorView.js` (com ícone, "meta individual: R$X" etc.) porque ele já tinha uma linha de texto própria mostrando a porcentagem, só não vinha do `ProgressBar`.
**Build verificado:** `✓ Compiled successfully`.

### Abas de gestão logo abaixo do herocard; lista de liderados; ícone de editar (sessão seguinte)
1. **As abas Placar/Colaboradores/Tarefas/Advertências/Premiações (dentro do `<EmpresaDashboard>`) agora ficam logo abaixo do herocard, em todos os usuários que têm herocard** (gerente e supervisor/sócio — master admin já estava assim, sem card nenhum entre o título e o `<EmpresaDashboard>`). Antes, em `GerenteView.js` os cards "Distribuição da meta em jogo" e "Ranking de vendas" apareciam ENTRE o herocard e o `<EmpresaDashboard>`; em `HierarchyHome.js` era o aviso de "só visualização" + "Metas da loja" + "Ranking de vendas" que ficavam nesse meio. Movi esses cards pra depois do `<EmpresaDashboard>` — em `GerenteView.js` foram unificados no mesmo bloco que já continha "Painel pessoal" (que já vinha depois); em `HierarchyHome.js`, como esses três cards também aparecem na aba Metas (acima das sub-abas Metas do mês/Lançamentos, papel diferente ali — resumo antes da gestão), duplicei o bloco: continua antes do `<EmpresaDashboard>` quando `tab === "metas"` (comportamento inalterado), e passou a vir depois quando `tab === "atividades"` (Início).
2. **"Definir liderados" (criar gerente, `Colaboradores()` em `EmpresaDashboard.js`) virou lista, não mais botões/pills.** O dropdown que abre ao clicar (adicionado numa sessão anterior) trocou o `flex flex-wrap` de pills arredondadas por uma lista vertical de verdade: cada colaborador é uma linha (`checkbox` + nome) dentro de uma caixa com borda, com scroll (`max-h-56 overflow-y-auto`) se a equipe for grande.
3. **Ícone de "editar valores" da meta trocado de `Coins` pra `Pencil`** (lápis) — ícone universal de edição, mais fácil de reconhecer do que a moeda (que ficava confuso ao lado do ícone de `Split` da distribuição). Os outros dois (`Split` pra distribuição, `Trash2` pra excluir) continuam iguais.
**Build verificado:** `✓ Compiled successfully`.

### Subtítulos removidos, seletor de mês ao lado do de loja, e cabeçalho escondido no iPhone (sessão seguinte)
1. **Subtítulo "Os níveis não somam: "removido de todos os cards "Metas da loja"/"Minhas metas"** (4 lugares: `EmpresaDashboard.js`, `ColaboradorView.js`, e as 2 cópias em `HierarchyHome.js` — ver nota da sessão anterior sobre por que esse card foi duplicado ali). Texto final: "Vale a meta real até ela ser batida, depois passa a valer a próxima, e assim sucessivamente." — mesma frase, só sem o prefixo.
2. **Subtítulo removido de todos os cards "Ranking de vendas"** (colaborador, gerente, supervisor/sócio ×2) — ficou só o título "Ranking de vendas — {mês}", sem a linha explicativa embaixo.
3. **Seletor de mês ao lado do seletor de loja, na visão supervisor/sócio (`HierarchyHome.js`).** Causa do bug: a saudação (`<h1>`) e o grupo loja+mês dividiam a mesma linha (`flex ... justify-between`), e o grupo loja+mês tinha `flex-wrap` — em telas estreitas não sobrava largura pros dois (seletor de loja + `MonthNav`, que sozinho já tem ~200px por causa do nome do mês por extenso + duas setas), então o `MonthNav` quebrava pra uma segunda linha, abaixo do seletor de loja. Corrigido: a saudação virou uma linha própria (largura cheia), e o grupo loja+mês virou a linha de baixo, também com largura cheia — sem competir por espaço com a saudação, os dois cabem lado a lado. O seletor de loja ganhou `flex-1 min-w-0` (ele encolhe se precisar) enquanto o `MonthNav` continua `shrink-0` (nunca encolhe, senão o texto do mês corta).
4. **Cabeçalho (perfil + botão "Sair") escondido atrás do notch/status bar no iPhone, impossível de clicar.** Causa: `app/layout.js` já configurava `viewportFit: "cover"` (necessário pro PWA ocupar a tela toda, sem barras brancas) e `statusBarStyle: "black-translucent"` (status bar transparente, sobrepondo o conteúdo) — mas o `<header>` fixo (`lib/AppShell.js`, `sticky top-0`) não tinha nenhum respiro pra compensar essa sobreposição, então nos iPhones com notch/Dynamic Island o topo do header (incluindo o botão de perfil e o "Sair") ficava fisicamente embaixo da área do notch/status bar, inacessível ao toque. Corrigido adicionando `padding-top: env(safe-area-inset-top)` ao `<header>` (classe Tailwind arbitrária `pt-[env(safe-area-inset-top)]`) — em dispositivos sem notch/safe area (Android, desktop, iPhones mais antigos) esse valor é `0px`, sem efeito nenhum; só entra em ação onde precisa. Confirmado que a regra CSS foi gerada corretamente no build (`grep` no CSS compilado).
   **Nota:** não foi possível testar em iPhone físico real dentro desta sessão — se ainda sobrar algum espaço errado (por exemplo, na barra de abas logo abaixo do header, ou no rodapé perto do home indicator), o mesmo padrão (`env(safe-area-inset-*)`) pode ser aplicado em `padding-bottom` de elementos fixos no rodapé, se algum existir.
**Build verificado:** `✓ Compiled successfully`.

### Seta do seletor de loja cortada (sessão seguinte)
Felipe reportou que a setinha do seletor de loja (visão supervisor/sócio, `HierarchyHome.js`) estava cortada. Causa raiz: a mudança anterior (deixar o seletor `w-full` dentro de um `label` `flex-1`, pra caber ao lado do `MonthNav`) fez o `<select>` esticar até a borda direita do pill, e a classe `!p-0` já existente removia todo o padding que o navegador reserva pra desenhar a setinha nativa — sem espaço de sobra, a seta nativa era desenhada bem em cima da curva do `rounded-full`, cortando visualmente a ponta de baixo. Corrigido de forma mais robusta: `appearance-none` no `<select>` (remove a seta nativa do navegador de vez, some com a inconsistência entre navegadores/SO) + um ícone `ChevronDown` (lucide-react) posicionado manualmente por cima (`absolute`, `pointer-events-none` pra não atrapalhar o clique no select por baixo), com `pr-4` no select garantindo espaço pro texto não passar por baixo do ícone. Mesmo padrão visual de outros dropdowns customizados já usados no app (Nova meta, Nova empresa, Definir liderados). Conferido que esse padrão (`!p-0` num select dentro de pill) não se repete em nenhum outro lugar do app — é o único seletor de loja da aplicação.
**Build verificado:** `✓ Compiled successfully`.

### Dashboard "Atividades por colaborador" na visão do gerente (sessão seguinte)
Felipe pediu, especificamente na visão do gerente, um dashboard mostrando de cada colaborador da equipe quantas atividades tem e quantas foram concluídas, com um indicador de status: vermelho se tem atividade atrasada, amarelo se tem pendente hoje (sem atraso), verde se está 100% em dia. Escopo explicitamente pedido só pra visão do gerente — não implementado em supervisor/sócio/master admin (podem pedir depois se quiserem o mesmo lá).
1. **Novo card "Atividades por colaborador — {mês}"** em `GerenteView.js`, na tela de Início, junto dos outros cards de apoio (depois das abas de gestão, mesmo lugar de "Ranking de vendas"/"Distribuição da meta em jogo").
2. **Cálculo (`loadStats`):** busca todas as `tasks` da equipe (`employee_id in (...)`) e todos os `task_completions` do mês selecionado ligados a essas tarefas, cruzando os dois no cliente (mesmo padrão de "buscar ids primeiro, depois filtrar por eles" já usado no resto do arquivo pro cálculo de `pendingToday` — evitei usar filtro `.in()` direto numa coluna de tabela unida via `tasks!inner(...)`, que não tinha precedente comprovado nesse código; o padrão existente só usa `.eq()` em coluna unida, então preferi a via seguramente já testada). Por colaborador: `expected` = total de linhas de `task_completions` no mês, `completed` = quantas com `completed=true`, `overdue` = quantas com `completed=false` e `completion_date` antes de hoje, `pendingToday` = quantas com `completed=false` e `completion_date` igual a hoje.
3. **Status (prioridade vermelho > amarelo > verde > cinza):** vermelho se `overdue > 0`; senão amarelo se `pendingToday > 0`; senão verde se `expected > 0` e `completed === expected`; senão cinza (neutro — sem tarefa nenhuma no mês ainda, ex.: tarefa nunca "semeada" porque ninguém abriu o app naquele dia, mesma limitação de semeadura preguiçosa já documentada nesse projeto). Indicador visual reaproveita o componente `Led` (`lib/Led.js`) já usado nas barras de progresso — mesma linguagem visual (🟢🟡🔴) do resto do app, com uma legenda de uma linha no cabeçalho do card.
4. **Verificação em dado real:** rodei a mesma agregação direto no Postgres (via SQL) pro gerente/equipe de teste — bateu exatamente com o esperado: Gustavo Belfort com 56/56 concluídas, 0 atrasadas, 0 pendentes hoje (verde); os outros 3 colaboradores da equipe com 0 tarefas no mês (cinza, sem dado ainda).
**Build verificado:** `✓ Compiled successfully`.

### Líder de vendas no herocard do supervisor/sócio; limite de nome menor; loja some em abas agregadas; abas apertadas no mobile do sócio (sessão seguinte)
1. **Líder de vendas adicionado ao herocard de supervisor/sócio (`HierarchyHome.js`).** Felipe pediu inicialmente pra igualar o herocard de sócio e supervisor (achando que eram diferentes) — esclarecido que os dois já eram idênticos (mesmo código, só a cor muda), e o que ele realmente queria era trazer pro herocard de sócio/supervisor o mesmo 5º número que o herocard do gerente já tem: **líder de vendas da loja selecionada**. Implementado reaproveitando o mesmo dado já calculado pro card "Ranking de vendas" (`storeRanking`, já ordenado por valor vendido desc) — `ranking[0]` é o líder, sem query nova. Grid do herocard passou de `grid-cols-2 sm:grid-cols-4` (4 números) pra `grid-cols-2 sm:grid-cols-3 lg:grid-cols-5` (5 números), mesmo padrão de grid escalável já usado no herocard do gerente pra 6 números.
2. **Limite de caracteres reduzido: nome completo 18 (antes 40), usuário (login) continua 20** (já estava certo, sem mudança) — em todos os formulários de criação/edição de nome, em `EmpresaDashboard.js` e `app/admin/page.js` (8 lugares ao todo). **Nota honesta pro Felipe:** 18 caracteres é bem apertado pra nome completo — nomes brasileiros compostos (ex.: "Felipe dos Santos Ribeiro" tem 24) não cabem inteiros. Segui a instrução à risca, mas se isso cortar nomes de verdade no cadastro, vale reconsiderar o número.
3. **Botão de selecionar loja escondido nas abas Rankings e Faturamento (`HierarchyHome.js`).** Pedido original era só pra aba Rankings, mas o mesmo raciocínio se aplica 1:1 à aba Faturamento (ambas agregam TODAS as lojas de uma vez — o seletor de loja nunca fez diferença nenhuma pro conteúdo dessas duas abas, só confundia). Estendi a mesma correção pra aba Supervisores também (não usa loja nem mês) — nela, a linha inteira de loja+mês some agora. O `MonthNav` (seletor de mês) continua aparecendo em Rankings/Faturamento porque essas duas abas de fato filtram por mês.
4. **Abas de navegação (Início/Metas/Rankings/Faturamento/Supervisores) apertadas com rolagem horizontal no mobile pro sócio.** Como o sócio tem 5 abas (os outros papéis têm 2 a 4), o texto+ícone no tamanho padrão não cabia na tela sem rolar. Reduzido em `AppShell.js` (componente compartilhado por todos os papéis — afeta todo mundo igual, só quem tem mais abas sente mais diferença): gap entre abas de `gap-4` pra `gap-2` no mobile, padding vertical de `py-3` pra `py-2.5`, fonte de `text-xs` pra `text-[10px]` no mobile (volta a `text-sm` no desktop), ícone de 16px pra 13px no mobile (volta a 16px — `sm:w-4 sm:h-4` — no desktop). A rolagem horizontal (`overflow-x-auto`) continua existindo como rede de segurança pra telas muito estreitas, mas na maioria dos aparelhos as 5 abas do sócio devem caber sem precisar rolar agora.
**Build verificado:** `✓ Compiled successfully`.

### Botão de loja idêntico pra sócio/supervisor; abas do sócio sem rolagem de vez (sessão seguinte)
1. **Botão de selecionar loja (tela Início) agora é visualmente idêntico pra qualquer sócio ou supervisor, independente da permissão real.** A única diferença que existia era o badge "visualização" aparecendo dentro do próprio botão quando `!canManageSelected` (supervisor com permissão só "ver" numa loja) — sócio nunca tinha esse badge porque sempre tem `permission: "gerenciar"` hardcoded. Removido o badge do botão; a informação de "só visualização" continua comunicada do mesmo jeito de sempre pelo aviso já existente ("Você tem apenas visualização nessa loja…", card separado que aparece junto dos outros cards de apoio) — não perde a informação, só para de fazer o botão em si mudar de forma dependendo de quem está logado.
2. **Abas de navegação (Início/Metas/Rankings/Faturamento/Supervisores) — resolvido de vez, sem depender de rolagem.** A tentativa anterior (só diminuir fonte/ícone) ainda deixava a rolagem horizontal como rede de segurança, sem garantia de que as 5 abas do sócio coubessem em qualquer aparelho. Desta vez apliquei em `AppShell.js` o MESMO padrão já comprovado que existe no `SubNav` (usado dentro do `EmpresaDashboard`, que já resolveu esse exato problema numa sessão anterior): cada aba vira `flex-1` no mobile (todas dividem a largura toda igualmente, não importa quantas sejam), com ícone empilhado em cima do texto (`flex-col`) e o texto com `truncate` (nunca quebra, corta com "…" se faltar espaço); no desktop (`sm:`) volta ao layout horizontal normal (ícone ao lado do texto, larguras naturais). Removido o `overflow-x-auto`/rolagem, porque agora as abas sempre cabem por construção — não é mais só "menor", é matematicamente garantido que N abas dividem 100% da largura disponível.
**Build verificado:** `✓ Compiled successfully`.

### Tarefa recorrente não pode valer para dias anteriores à sua criação
**Bug:** ao criar uma tarefa diária (ou semanal/única) hoje, a regra de recorrência em `isTaskDueOn` (`lib/date.js`) não sabia a partir de quando a tarefa existia — então, ao navegar pra dias passados (antes da tarefa ter sido criada), o app calculava a tarefa como "devida" naquele dia passado também, o que não faz sentido (a tarefa nem existia ainda) e podia inflar contagens de "atrasada"/"pendente" incorretamente para dias anteriores à criação.

**Fix:**
1. Migração no banco (`add_task_start_date`, projeto `fjscwmrjkxgygdzwwrdh`): nova coluna `tasks.start_date date not null default current_date`, com backfill das linhas existentes a partir de `created_at::date`. Confirmado via SQL que o backfill bateu certinho com a data de criação de cada tarefa existente.
2. **Por que uma coluna nova e não reaproveitar `created_at`:** `created_at` é `timestamptz` (armazenado em UTC), enquanto todo o resto do app calcula "hoje" no fuso de São Paulo (`todayStr()` em `lib/date.js`). Uma tarefa criada às 23h30 (horário de Brasília) vira 02h30 UTC do dia seguinte — se comparasse `created_at.slice(0,10)` direto com as datas locais de SP, teria um bug de "um dia a mais" bem perto da meia-noite. Por isso `start_date` é uma coluna `date` pura, preenchida pelo cliente com `todayStr()` no momento do insert — o mesmo padrão já usado em `entry_date`, `completion_date`, `warning_date` e `once_date`.
3. `isTaskDueOn(task, dateStr)` em `lib/date.js` ganhou um guard no topo: `if (task.start_date && dateStr < task.start_date) return false;` — antes de qualquer checagem de recorrência. Como essa função é o único lugar que decide se uma tarefa "vale" num dia, o fix cobre automaticamente todos os pontos de chamada (`EmpresaDashboard.js`, `GerenteView.js`, `ColaboradorView.js`), desde que a query que busca a tarefa inclua a coluna `start_date` — os dois `select("*")` em `EmpresaDashboard.js` e `ColaboradorView.js` já trazem a coluna automaticamente; a query mais estreita em `GerenteView.js` (usada pro cálculo de "pendente hoje") foi ajustada de `select("id, recurrence_type, weekday, once_date")` para incluir `start_date`.
4. `addTask` em `EmpresaDashboard.js` agora inclui `start_date: todayStr()` no objeto `recurrenceFields`, que é espalhado (`...recurrenceFields`) tanto no insert único quanto no insert em massa (replicar pra todos os colaboradores da loja) — toda tarefa nova, a partir de agora, nasce com `start_date` igual ao dia da criação.
5. O card "Atividades por colaborador" (adicionado nesta mesma sessão) não foi afetado — ele conta só com base nas linhas de `task_completions` que já existem (modelo de "semeadura preguiçosa"), não usa `isTaskDueOn`.

**Verificação:** confirmado via SQL que tarefas recém-criadas hoje (13/07 e antes) têm `start_date` batendo com `created_date`; `get_advisors(security)` rodado após a migração não mostrou nenhum alerta novo além dos já conhecidos (buckets públicos com listagem e funções SECURITY DEFINER, ambos pré-existentes).
**Build verificado:** `✓ Compiled successfully`.

### Modal de celebração ao bater cada nível de meta (colaborador)
**Pedido:** um modal precisa aparecer toda vez que o colaborador bate a meta do mês — bateu o 1º nível (ex.: "Meta") aparece, bateu o 2º nível (ex.: "Super Meta"), se existir, aparece de novo, e assim por diante pra quantos níveis a loja tiver configurado.

**Onde plugou:** `lib/ColaboradorView.js`, que já tinha um mecanismo idêntico pronto pra outra coisa (modal de "concluiu 100% das tarefas de hoje", com `Confetti` + card `PartyPopper` + flag no `localStorage` pra não repetir) — reaproveitei exatamente o mesmo padrão visual e de estado, só trocando o gatilho e o texto.

1. **Detecção:** `goals` (estado já existente) é a lista de níveis de meta do colaborador no mês (`sales_goals` + `sales_goal_allocations`, já vinha ordenada por `store_total` — ou seja, do nível mais baixo pro mais alto). Um novo `useEffect` roda sempre que `goals`, `soldSoFar` (total vendido no mês) ou o mês mudam: para cada nível em que `soldSoFar >= allocation.amount`, verifica uma flag no `localStorage` (`zmeta_goalhit_{profile.id}_{mês}_{goal.id}`) — se ainda não tiver sido marcada, marca e enfileira aquele nível pra celebrar.
2. **Fila, não só um modal:** os níveis batidos entram numa fila (`goalCelebrations`, array de estado). Se o colaborador pular direto de R$0 pra um valor que bate dois ou três níveis de uma vez (um lançamento grande, ou vários lançamentos somados antes de abrir o app), os modais aparecem um atrás do outro — o botão do modal mostra "Bora pra próxima!" quando ainda tem mais na fila, e "Show de bola!" no último.
3. **Só pro mês corrente e só pro próprio colaborador:** não dispara ao navegar por meses passados (bater uma meta de mês fechado não devia gerar pop-up agora) nem quando o gerente está "vendo como" aquele colaborador (`viewedByManager`) — mesma regra já usada no modal de tarefas 100%.
4. **Primeira vez que a funcionalidade roda:** quem já tinha batido algum nível de meta antes dessa atualização vai ver o(s) modal(is) na próxima vez que abrir o app (porque a flag ainda não existe no `localStorage` dele) — é esperado, é um efeito colateral normal de introduzir uma flag nova, e só acontece uma vez por pessoa.
5. Os dois modais (tarefas 100% e meta batida) não aparecem sobrepostos — o de meta só renderiza quando o de tarefas não está aberto.

**Build verificado:** `✓ Compiled successfully`.

### Botão "redefinir senha" faltando na gestão de loja (gerente/colaborador)
**Bug reportado:** Felipe foi editar um colaborador e não encontrou a opção de redefinir a senha — função que "já existia".

**Causa raiz:** a função de fato já existia de ponta a ponta (rota `/api/admin/update-employee`, campo `resetPassword`, redefine para a senha padrão `123456789` via `admin.auth.admin.updateUserById` + marca `must_change_password: true`), mas só tinha botão na UI dentro de `EditUser` (`app/admin/page.js`), usado exclusivamente na tela `EmpresaDetail` do **master_admin** (visão agregada da empresa inteira, fora de uma loja específica). O componente `Colaboradores()` em `lib/EmpresaDashboard.js` — usado por **todos** os papéis (gerente/supervisor/sócio/master_admin) ao entrar numa loja específica e gerenciar a equipe dali — nunca teve esse botão, nem na lista de gerentes nem na de colaboradores. Como sócio/supervisor só gerenciam colaboradores por esse caminho (não têm acesso ao `EmpresaDetail`, que é exclusivo do master_admin), a função ficava inacessível pra eles na prática, e mesmo o master_admin só a via se entrasse pelo caminho certo.

**Fix:** adicionado botão ícone-only (`KeyRound`, com `title`/`aria-label`, mesmo padrão de decluttering já usado no resto do app) em `Colaboradores()` (`lib/EmpresaDashboard.js`):
- Na lista de **gerentes**: visível quando `canManageTeams` (master_admin/sócio/supervisor — mesmo gate que já protege editar/desativar/excluir gerente).
- Na lista de **colaboradores**: visível quando `canEdit` (todos exceto `"leitor"` — inclui gerente, mantendo paridade com os botões editar/desativar/excluir que colaborador já tinha ali).

Nova função `resetPassword(user)` chama a mesma rota `/api/admin/update-employee` já existente com `{ employeeId, resetPassword: true }`, com `window.confirm` avisando a senha padrão antes de executar e `alert` de confirmação/erro depois — mesmo padrão de UX já usado em `EditUser`.

**Fora do escopo, sinalizado mas não implementado:** a tela "Supervisores" do sócio (`HierarchyHome.js`, gerencia contas de supervisor) também não tem botão de redefinir senha — só tem ativar/desativar. Não mexi porque o pedido foi específico pra gerente/colaborador; avisar se quiser o mesmo ali.

**Build verificado:** `✓ Compiled successfully`.

### Remoção de "faltam N dia(s) no mês" no subtítulo da aba Metas (colaborador)
Felipe pediu pra tirar o texto "faltam 18 dia(s) no mês" do subtítulo abaixo do título "Metas — {mês}" na visão do colaborador. Em `ColaboradorView.js`, a linha "Vendido no mês até {data}: {valor}" tinha um sufixo condicional (`isCurrentMonth ? " · faltam N dia(s) no mês" : " · mês fechado"`) — removida só a parte de "faltam N dias", mantido "· mês fechado" quando o mês visualizado já passou (informação diferente, não pedida pra sair). Único lugar do app com esse texto — não existe equivalente nos dashboards de gerente/supervisor/sócio. O contador de dias restantes em si continua no herocard (número grande "{remaining}"), só esse subtítulo específico da aba Metas perdeu a frase.
**Build verificado:** `✓ Compiled successfully`.

### Dashboard Tarefas: removido pausar/reativar, botões viram ícone, e excluída some de vez da lista
Três pedidos do Felipe na lista de "Tarefas" cadastradas (`Tarefas()`, `lib/EmpresaDashboard.js` — compartilhado por gerente/supervisor/sócio/master_admin):
1. **Botão "pausar"/"reativar" removido.** Não fazia sentido pro fluxo real de uso — a função `toggleActive(t)` foi removida do componente (só existia ali; não afeta as outras funções de mesmo nome em `Colaboradores()`, que são de outro escopo).
2. **Botões viraram ícone-only** (`Pencil` editar / `Trash2` excluir, com `title`/`aria-label`), no mesmo padrão de decluttering já usado em `Metas()`.
3. **"Editar" agora existe de verdade** (antes não existia nenhuma forma de editar uma tarefa já criada, só criar/pausar/excluir). Implementado inline (mesmo padrão de `Colaboradores()`: clique no lápis troca a linha por um input + salvar/cancelar) — só o **título** é editável por ora; recorrência (diária/semanal/única, dia/data) não tem edição, precisaria excluir e recriar se for pra mudar isso. Se Felipe quiser editar recorrência também, é extensão natural do mesmo padrão.
4. **Bug real corrigido: tarefa excluída continuava aparecendo na lista, riscada.** A lista sempre renderizou `myTasks` (todas, ativas e inativas), com `line-through` pra inativas — como "excluir" já fazia soft-delete (`active=false`, preservando histórico), a tarefa nunca sumia visualmente, só ficava riscada pra sempre. Como o botão de pausar/reativar saiu (item 1), `active=false` passou a significar exclusivamente "excluída" — não existe mais nenhum caminho pra desativar uma tarefa sem ser via exclusão. Por isso a lista trocou de `myTasks` pra `myActiveTasks` (já existia, usada em outro lugar do componente): tarefa excluída agora some de vez da lista de cadastro.
   **A lógica de dados por trás do "excluir" não mudou e continua correta** (reconfirmado, é a mesma regra já documentada na seção 11 desde a primeira implementação): apaga só `task_completions` com `completed = false` **e** `completion_date >= hoje` (pendências de hoje em diante, que ainda nem deveriam ter acontecido); dias já concluídos (`completed = true`, qualquer data) e dias passados perdidos (`completed = false`, data anterior a hoje) nunca são tocados — o hard delete da tabela `tasks` continua nunca acontecendo, porque a FK `task_completions.task_id → tasks.id` é `ON DELETE CASCADE` e apagaria esse histórico junto. "Excluir mesmo" (o que Felipe pediu) foi resolvido no nível de **exibição** (a tarefa some da tela), não no nível de dado — apagar o dado de verdade quebraria relatórios/comissão de meses fechados, prática já rejeitada e documentada antes neste mesmo projeto.
**Build verificado:** `✓ Compiled successfully`.

### Limite de 18 caracteres no nome da tarefa
`maxLength={18}` adicionado nos dois inputs de título de tarefa em `Tarefas()` (`lib/EmpresaDashboard.js`): o campo "nome da tarefa" do formulário de criação e o input de edição inline (adicionado na sessão anterior). Mesmo limite já usado pra nome completo em outros formulários do app. **Nota:** client-side só — o insert/update vai direto pro Supabase (não passa por rota de API), então o mesmo limite client-side-only já documentado pros nomes de usuário se aplica aqui. 18 caracteres é apertado pra descrever uma tarefa (ex.: "Organizar vitrine" já bate o limite); se cortar descrições no uso real, vale reconsiderar.
**Build verificado:** `✓ Compiled successfully`.

### Seletor de dia da semana (criar tarefa semanal) trocado de `<select>` nativo por pills
Felipe pediu pra melhorar a aparência do seletor de dia da semana ao criar tarefa com recorrência "1 dia na semana" (`Tarefas()`, `lib/EmpresaDashboard.js`). Era um `<select>` HTML nativo genérico, destoando do resto do formulário — que logo acima já usa botões-pill (`rounded-full border-2`) pra escolher o tipo de recorrência (diária/semanal/única). Trocado pelo mesmo padrão: 7 botões-pill (`WEEKDAY_LABELS`, `text-xs font-bold px-3 py-1.5 rounded-full border-2`, ativo em roxo) com `flex-wrap`, idêntico visualmente ao seletor de recorrência logo acima.
**Build verificado:** `✓ Compiled successfully`.

### Subtítulo do "Checklist do dia" removido
Removido o `<p>` explicativo abaixo do título "Checklist do dia" em `Tarefas()` (`lib/EmpresaDashboard.js`) — "Você pode marcar ou corrigir tarefas de qualquer dia..." (quem edita) / "Só aparecem aqui as tarefas que valem nesse dia." (leitor). Ambas as variantes saíram, não só uma.
**Build verificado:** `✓ Compiled successfully`.

### Padronização de todos os `<select>` do app + reversão do seletor de dia da semana
Felipe não gostou da versão anterior do seletor de dia da semana (botões-pill, sessão passada) — pediu pra voltar ao modelo `<select>` e, junto, padronizar **todos** os seletores do app com a mesma aparência do seletor de loja que sócio/supervisor já tinham (`HierarchyHome.js`): pill branca com borda roxa (`border-2 border-purple/30`), `rounded-full`, `shadow-soft`, seta customizada (`ChevronDown` posicionado manualmente por cima de um `<select>` nativo com `appearance-none`, já que a seta nativa do navegador não tem como ser estilizada de forma consistente entre navegadores/SO — mesma razão documentada na sessão que corrigiu a seta cortada do seletor de loja).

1. **Componente novo `lib/SelectField.js`.** Em vez de copiar esse markup em cada lugar (11 seletores no total, espalhados em 3 arquivos), extraí um componente único reaproveitando exatamente o padrão que já existia no seletor de loja — resolve na raiz, como já é convenção deste projeto pra padrões repetidos. Props: `value`/`onChange`/`children` (options/optgroups, igual um select nativo), `icon` (ícone opcional à esquerda, ex.: `Store` no seletor de loja — a maioria dos outros seletores não usa), `className` (controla largura/posicionamento do wrapper pill) e `selectClassName`/`disabled`/resto repassado direto pro `<select>` interno.
2. **Seletor de dia da semana revertido pro `<select>`** (`Tarefas()`, `lib/EmpresaDashboard.js`) — os botões-pill da sessão anterior saíram, agora usa `<SelectField>` dentro do mesmo `max-w-[220px]` de antes.
3. **Todos os 11 seletores do app migrados pro `SelectField`:**
   - `HierarchyHome.js`: seletor de loja (sócio/supervisor) — agora consome o próprio componente novo em vez de ter o markup inline (era a referência original).
   - `EmpresaDashboard.js` (6 seletores): "Gerente (equipe)" no cadastro de colaborador, "Gerente" na edição inline de colaborador, "Colaborador ou gerente" em Tarefas/Advertências/Premiações (3 ocorrências, mesmo padrão com `optgroup`), "Colaborador" em Lançamentos, e o de dia da semana (item 2).
   - `app/admin/page.js` (4 seletores): "De"/"Até" (intervalo de mês no financeiro), e "Loja" nos formulários de criar gerente e criar colaborador (2 ocorrências).
4. **Import órfão removido:** `ChevronDown` não é mais importado direto em `HierarchyHome.js` (a seta agora mora dentro de `SelectField.js`); `Store` continua importado ali porque ainda é usado em outros dois lugares do arquivo.
**Ressalva sincera:** o `SelectField` tem altura fixa (`py-1.5`) igual ao seletor de loja original — na maioria dos formulários ele fica sozinho na própria célula/linha (label em cima, campo embaixo), mas em duas linhas de edição inline (`Colaboradores()`, onde o select fica ao lado de inputs de texto `!py-1.5`) a altura já bate certinho porque esses inputs já usavam esse mesmo `!py-1.5` de override. Não identifiquei nenhum lugar onde a altura ficaria visivelmente desalinhada, mas vale um olhar real no app depois do deploy.
**Build verificado:** `✓ Compiled successfully`.

### Dois bugs reais no card "Atividades por colaborador" (gerente) + título sem mês
Felipe reportou dois números claramente errados no card "Atividades por colaborador" (`GerenteView.js`, tela de Início do gerente): Gustavo Belfort tinha só 4 tarefas mas aparecia "52/60"; e os outros colaboradores da equipe, mesmo tendo tarefas cadastradas, apareciam zerados. Investigado com `execute_sql` direto no Supabase (projeto `fjscwmrjkxgygdzwwrdh`) antes de mexer no código, confirmando as duas causas raiz:

1. **"52/60" do Gustavo — completions de tarefas EXCLUÍDAS entrando na conta.** O cálculo somava `task_completions` de TODAS as tarefas já vinculadas ao colaborador, ativas ou não (`.select("id, employee_id").in("employee_id", empIds)` sem `.eq("active", true)`). Gustavo tinha testado a criação/exclusão de tarefas mais cedo hoje: 4 tarefas excluídas (`active=false`) ainda tinham 14 dias de histórico cada (56 linhas, 52 concluídas) — exatamente o "52/60" que apareceu, sem nenhuma relação com as 4 tarefas realmente ativas dele hoje. Confirmado via SQL: `56 linhas + 4 tarefas ativas de hoje = 60`, `52 concluídas + 0 de hoje = 52`. Isso é consequência direta do design já documentado (exclusão preserva histórico via soft-delete, `active=false`) — só faltava esse dashboard específico filtrar por tarefa ativa, o que os outros pontos do app já faziam.
2. **Colaboradores com tarefa cadastrada aparecendo zerados — dependia de completions já "semeadas".** O "esperado" (`expected`) vinha de contar linhas de `task_completions` já existentes no mês. Como o modelo é de semeadura preguiçosa (a linha só nasce quando alguém abre o checklist daquele dia — o próprio colaborador logando, ou um gerente/admin entrando na aba Tarefas e selecionando aquela pessoa), um colaborador cujo checklist ninguém abriu ainda no dia aparecia "0/0" mesmo tendo tarefas ativas valendo hoje. Confirmado via SQL: Janailson, Lais e Marianna tinham 5-6 tarefas ativas cada (todas criadas hoje, `start_date = 2026-07-14`) e **zero** linhas de `task_completions` no mês.

**Fix (`loadStats` em `GerenteView.js`):** o cálculo do card deixou de contar linhas de `task_completions` existentes e passou a calcular, pra cada colaborador, os **dias em que cada tarefa ATIVA realmente valia** no intervalo (do início do mês até hoje, ou até o fim do mês se for mês fechado), usando `isTaskDueOn` — mesma função central de recorrência já usada no resto do app — e só então cruza com as completions que já existem pra saber o que foi feito. Isso resolve os dois bugs de uma vez: tarefa excluída não entra mais (query já filtra `active=true`), e colaborador sem completions "semeadas" ainda mostra o número certo de tarefas esperadas hoje (o "esperado" nasce da regra de recorrência, não da tabela de completions). `daysInMonth` importado de `lib/date.js` pra montar o intervalo de dias.

**Verificado contra os dados reais depois do fix (simulação manual da nova lógica com os dados já levantados):** Gustavo passa a mostrar **0/4** (4 tarefas diárias ativas valendo hoje, nenhuma marcada ainda — amarelo/pendente), e Janailson/Lais/Marianna passam a mostrar **0/5** ou **0/6** dependendo da quantidade de tarefas diárias que cada um tem (a tarefa semanal "Troca do VM", quinta-feira, não conta pra hoje que é terça) — todos corretamente amarelos (pendente hoje) em vez de zerados.

**Título do card simplificado:** "Atividades por colaborador — {mês}" virou só "Atividades por colaborador" — o card já não tem seletor de mês próprio (segue o mês selecionado no resto da tela), então o sufixo era redundante.

**Nota de escopo:** não mexi no cálculo separado de "Atividades pendentes" do herocard (bloco logo acima, linhas ~108-128) — ele já filtrava corretamente por `active=true` e por `isTaskDueOn`, então não tinha os mesmos dois bugs. Ficou uma pequena duplicação de lógica entre os dois blocos (ambos calculam "o que vale hoje" de formas parecidas) — dá pra unificar num refactor futuro se quiser, não fiz agora pra manter a mudança focada só no que estava quebrado.
**Build verificado:** `✓ Compiled successfully`.

### Bug real: sócio não conseguia redefinir senha (nem criar/editar/excluir) por causa de checagem de permissão errada em 4 rotas de API
Felipe reportou o erro "Você não tem permissão de gerenciar essa loja" ao tentar redefinir a senha de um colaborador logado como **sócio**. Investigado direto no Supabase (`execute_sql`, projeto `fjscwmrjkxgygdzwwrdh`) antes de mexer em código.

**Causa raiz:** as rotas `update-employee`, `delete-employee`, `create-employee` e `create-gerente` (todas em `app/api/admin/`) tratavam sócio e supervisor como o mesmo caso (`isHierarquia`) e, pra ambos, exigiam uma linha em `loja_access` com `permission = 'gerenciar'` pra liberar a ação. Isso é certo pra **supervisor** (que realmente tem permissão granular por loja, via `loja_access`), mas está **errado pra sócio** — sócio gerencia TODAS as lojas da própria empresa de forma implícita, nunca depende de `loja_access` (essa regra já existe corretamente na função `can_manage_loja()` do Postgres, fonte de verdade das políticas RLS: `is_socio() and exists (select 1 from lojas where id = p_loja and empresa_id = my_empresa_id())`, sem tocar em `loja_access`). As 4 rotas de API tinham reimplementado essa checagem de permissão de forma manual e incompleta, sem essa distinção.

Confirmado o efeito prático via SQL: o sócio de teste ("Felipe teste", empresa com 2 lojas — ArmyBR Anália Franco e ArmyBR Iburapuera) tinha só **1** linha em `loja_access`, com `permission = 'ver'` (não "gerenciar"), pra apenas 1 das 2 lojas. Ou seja, mesmo a única loja com alguma linha já bloquearia a ação (permissão errada), e a segunda loja não tinha linha nenhuma. Na prática, o sócio estava sem conseguir gerenciar colaboradores/gerentes em nenhuma das duas lojas por essa API — apesar de RLS no banco (que usa `can_manage_loja()` corretamente) sempre ter permitido as escritas de verdade. Ou seja, o problema era só na camada da API, que barrava a ação antes mesmo de chegar no banco.

**Fix:** criado `lib/serverPermissions.js`, com a função `hierarquiaCanManageLoja({ callerClient, callerProfile, userId, lojaId, lojaEmpresaId })` — sócio libera direto comparando `lojaEmpresaId === callerProfile.empresa_id` (sem tocar `loja_access`); supervisor mantém a checagem original em `loja_access.permission = 'gerenciar'`. As 4 rotas foram atualizadas pra usar esse helper único em vez de reimplementar a checagem cada uma à sua maneira — resolve na raiz e evita esse mesmo bug se aparecer uma 5ª rota no futuro. Rota `hierarchy-access` (usada só pelo sócio pra CONCEDER acesso a supervisores) foi checada e não tinha esse bug — ali a checagem já era só por `empresa_id`, sem depender de `loja_access` pro próprio sócio.
**`get_advisors(security)` rodado após a mudança:** nenhum alerta novo além da baseline já conhecida (buckets públicos, funções SECURITY DEFINER, leaked password protection).
**Build verificado:** `✓ Compiled successfully`.

### Novo dashboard "Ranking de lojas" (sócio/supervisor)
Felipe pediu um dashboard mostrando o ranking das lojas liberadas pra visão do sócio/supervisor, aparecendo tanto na tela Início quanto na aba Rankings.

**Métrica escolhida:** valor vendido no mês por loja, reaproveitando o dado que a aba Faturamento já calculava (`faturamento.byLoja`, em `HierarchyHome.js` — já vinha ordenado por total desc, só nunca tinha sido exibido fora da tabela da aba Faturamento). Não criei query nova nem RPC — só reaproveitei o state já existente.

1. **Componente novo `LojaRankingCard`** (`HierarchyHome.js`) — mesmo padrão visual do card "Ranking de vendas" já existente (círculo numerado, 1º lugar em destaque laranja, nome truncando, valor à direita), só que o item é a loja inteira em vez de um colaborador.
2. **Aba Rankings:** vira o primeiro card da aba, acima da grade dos 4 rankings de colaboradores (Top vendedores/Mais premiados/Líder de tarefas/Mais comissionados) — `RankingsTab` ganhou a prop `byLoja`, e o loading da aba passou a esperar tanto `rankingsLoading` quanto `faturamentoLoading` (antes só esperava o primeiro; como os dois carregam em paralelo desde sessões anteriores, sem essa mudança o card de lojas podia piscar vazio por uma fração de segundo).
3. **Tela Início:** card adicionado logo depois de "Ranking de vendas" (o de colaboradores da loja selecionada), só quando `lojas.length > 1` — com 1 loja só, um ranking de 1 item não agrega nada e só teria poluído a tela (achei melhor não mostrar do que mostrar um "ranking" com um item único). Na aba Rankings o card aparece sempre, mesmo com 1 loja, porque ali a aba inteira já é dedicada a isso.
**Verificado com dado real:** SQL direto no Supabase confirmou as duas lojas da empresa de teste com valores bem diferentes (R$113.065,17 vendido numa loja, R$0 na outra), então o ranking teria conteúdo de verdade pra mostrar, não uma tela vazia.
**Build verificado:** `✓ Compiled successfully`.

### Auditoria completa das barras de tarefas: bug real (mesmo padrão de hoje) presente em 3 lugares + RPC SQL, título renomeado, Configurações mudou de aba, e vazamento de cards entre sub-abas corrigido
Felipe reportou que a "Barra geral da equipe — Julho de 2026" mostrava 96,7% mesmo com vários colaboradores com tarefas pendentes, pediu pra renomear o título pra "Barra Geral de Atividades", pediu auditoria de TODAS as barras de tarefas e de vendas do app, pediu pra mover o card "Configurações" do Início pra dentro da aba Advertências, e reportou que trocar de sub-aba (ex.: Advertências) continuava mostrando cards de outras sub-abas (Metas da loja, Ranking de vendas).

1. **Bug real confirmado e corrigido em 3 lugares + 1 função SQL — mesmo padrão de causa raiz já corrigido hoje mais cedo em `GerenteView.js` (seção "Atividades por colaborador"), só que dessa vez afetando a barra principal usada por TODOS os papéis:**
   - **Causa raiz (idêntica nas 4 ocorrências):** o "esperado" de cada colaborador vinha de contar linhas de `task_completions` já existentes no mês, em vez de calcular quantos dias a tarefa realmente valia pela recorrência. Como completions só nascem quando alguém abre o checklist daquele dia ("semeadura preguiçosa"), um colaborador cujo checklist ninguém abriu ainda ficava com `expected = 0` — e `calcIndividualPct` trata `expected = 0` como **100%** (nota máxima por falta de dado, não por mérito real), inflando a média da equipe pra cima. Além disso, nenhum dos 4 lugares filtrava tarefas por `active = true`, então o histórico de tarefas já **excluídas** também entrava na conta, distorcendo ainda mais o número.
   - **Confirmado via SQL antes de mexer no código** (projeto `fjscwmrjkxgygdzwwrdh`): a equipe de teste tinha 4 colaboradores, cada um com 4-6 tarefas diárias ativas criadas no mesmo dia (nenhuma ainda concluída) — o valor real da barra deveria ser **0%** (ninguém tinha marcado nada ainda), não 96,7%.
   - **Corrigido em:**
     1. `EmpresaDashboard.js` (`Placar()`/`teamPct` — a barra que Felipe estava olhando, usada por gerente e master_admin).
     2. `HierarchyHome.js` (`loadRankings()`, card "Líder de tarefas concluídas" na aba Rankings de sócio/supervisor).
     3. Função Postgres `get_team_progress(p_month, p_loja)` — usada pela "Barra Geral de Atividades" que o **colaborador** vê na própria tela. Reescrita com `generate_series` pra calcular os dias em que cada tarefa ativa valia (mesma regra de `isTaskDueOn`: diária todo dia, semanal só no seu dia da semana, única só na sua data, nunca antes de `start_date`), cruzando só depois com as completions que já existem. Ganhou um 3º parâmetro `p_today` (a função agora se chama com `p_month, p_loja, p_today`) — passado pelo client via `todayStr()` (fuso America/Sao_Paulo) em vez de usar `current_date` do Postgres, pra não correr risco de descasamento de fuso perto da meia-noite.
     4. **Armadilha na migração da RPC:** `CREATE OR REPLACE FUNCTION` com uma assinatura diferente (parâmetro novo) não substitui a função antiga — cria uma segunda sobrecarga, porque o Postgres identifica função pela assinatura completa. A versão antiga de 2 argumentos (com o bug) continuou existindo até eu rodar um `DROP FUNCTION` explícito numa segunda migração. **Lição pro futuro: sempre que uma migração mudar a assinatura de uma função existente (não só o corpo), conferir com `pg_get_function_identity_arguments` se não sobrou uma versão antiga esquecida.**
   - **Já corrigido antes de hoje (não precisou de mudança):** o cálculo de "Atividades pendentes" no herocard do gerente (linhas ~108-128 de `GerenteView.js`) já filtrava por `active=true` e `isTaskDueOn` corretamente.
   - **Reaproveitado um helper novo `daysElapsedInMonth(monthArg, todayArg)` em `lib/date.js`** — centraliza a lógica de "lista de dias já ocorridos do mês" (do dia 1 até hoje, ou até o fim do mês se for mês fechado) que já tinha sido escrita inline em `GerenteView.js` mais cedo hoje; refatorei esse código pra usar o helper também, eliminando a duplicação.
   - **Barras de VENDAS auditadas e sem bug equivalente:** todas calculam vendido/meta a partir de `sales_entries` (lançamentos de venda), que não tem o problema de "semeadura preguiçosa" — todo lançamento de venda é criado explicitamente pelo colaborador/gerente, não existe conceito de "dia esperado sem registro". Não precisaram de mudança.
2. **Título renomeado:** "Barra geral da equipe" → **"Barra Geral de Atividades"**, em `EmpresaDashboard.js` (com o mês no título) e em `ColaboradorView.js` (mesmo conceito, sem mês no título — mantive a consistência entre os dois, já que é o mesmo dado só visto de ângulos diferentes).
3. **Card "Configurações" (desconto por advertência / meta da barra geral / premiação mensal) saiu do Início e foi pra dentro da aba Advertências**, em `EmpresaDashboard.js` — vale pra gerente e master_admin (os únicos papéis que já viam esse card; supervisor/sócio continuam sem vê-lo, como já era). Os estados do formulário foram renomeados (`cfgPenalty`/`cfgThreshold`/`cfgPrize`) pra não colidir com o campo "Desconto (%)" que já existia em Advertências pra cada advertência individual lançada.
4. **Vazamento de cards entre sub-abas corrigido.** Causa raiz: a sub-navegação Placar/Colaboradores/Tarefas/Advertências/Premiações (`atSub`) vive **dentro** de `EmpresaDashboard.js`, como estado interno — os componentes-pai (`GerenteView.js`, `HierarchyHome.js`) que renderizam cards de apoio ao redor do `<EmpresaDashboard>` (Distribuição da meta em jogo, Ranking de vendas, Atividades por colaborador, Painel pessoal, Metas da loja, Ranking de lojas) só sabiam a aba EXTERNA (Início/Metas/Rankings/Faturamento), nunca a sub-aba interna — então esses cards ficavam sempre visíveis dentro de "Início", não importa se o usuário tivesse clicado em Colaboradores/Tarefas/**Advertências**/Premiações.
   - **`atSub` virou estado controlável** em `EmpresaDashboard.js` (props opcionais `atSub`/`onAtSubChange` — sem elas, cai no comportamento antigo de estado interno, então o master_admin, que não tem cards de apoio fora daqui, não precisou de nenhuma mudança).
   - **`GerenteView.js` e `HierarchyHome.js`** passaram a manter `atSub` no próprio estado, repassar pro `<EmpresaDashboard>`, e usar `atSub === "placar"` como condição extra pros cards de apoio (além da aba externa já ser "Início"/atividades).
   - **Exceção cuidadosa em `HierarchyHome.js`:** o aviso "Você tem apenas visualização nessa loja" (pra supervisor com permissão só de ver) foi deixado **fora** do bloco `atSub === "placar"` — ele é relevante em qualquer sub-aba (é justamente nas sub-abas de edição, tipo Colaboradores/Tarefas, que essa informação mais importa), então continua aparecendo em todas.
**Build verificado:** `✓ Compiled successfully`. `get_advisors(security)` rodado depois de tudo — mesma baseline conhecida, sem alertas novos (inclusive confirmado que sobrou só 1 versão de `get_team_progress` depois do `DROP FUNCTION`).

### "Barra do mês" renomeada para "Barra Individual"
Título do card de barra individual do colaborador (`ColaboradorView.js`, ao lado da "Barra Geral de Atividades" renomeada na sessão anterior) trocado de "Barra do mês" pra "Barra Individual" — só o texto, sem mudança de cálculo. Único lugar do app com esse título.
**Build verificado:** `✓ Compiled successfully`.

### "Barra Geral de Atividades" ainda com o mês no título na visão do gerente/master_admin
Na renomeação anterior ("Barra geral da equipe" → "Barra Geral de Atividades"), o título em `EmpresaDashboard.js` (visão gerente/master_admin) manteve o sufixo "— {mês}" por descuido, enquanto o mesmo card em `ColaboradorView.js` (visão colaborador) já tinha ficado sem. Felipe apontou a inconsistência e reforçou a regra de ouro do projeto: **dashboards compartilhados entre papéis precisam ser visualmente idênticos**. Removido o sufixo de mês também em `EmpresaDashboard.js` — título agora é só "Barra Geral de Atividades" em qualquer papel que o veja (gerente, master_admin, colaborador).
**Build verificado:** `✓ Compiled successfully`.

### Card "Lançar valor vendido" (colaborador): título, data limitada a ontem, e modal de folga em venda R$0
Três ajustes no card de lançamento de venda em `ColaboradorView.js`:
- **Título:** "Lançar valor vendido ontem" → "Lançar valor vendido" (o texto "ontem" ficou redundante depois do ajuste abaixo).
- **Calendário não permite mais lançar venda de hoje:** `max` do input de data mudou de `today` para `yesterdayStr(today)` (helper já existente em `lib/date.js`), e o valor inicial do estado `entryDate` também passou a nascer em `yesterdayStr(todayStr())` em vez de hoje — só é possível lançar/corrigir venda até o dia anterior.
- **Modal de folga em venda R$0,00:** `saveEntry` foi dividido em `saveEntry` (handler do submit) + `doSaveEntry(isFolga)` (upsert de fato). Quando o valor lançado é exatamente `0`, em vez de salvar direto, abre um modal perguntando "Foi um dia de folga?" com dois botões: "Não foi folga" (salva normalmente, sem tocar em tarefas) e "Sim, foi folga" (salva o lançamento de R$0 **e** marca automaticamente como concluídas (`completed: true`, `completed_at: now()`) todas as tarefas do colaborador que valem naquela data específica — via `isTaskDueOn` sobre o array `tasks` já carregado, upsert em `task_completions` com `onConflict: "task_id,completion_date"`, mesmo padrão já usado em `toggleDayTask` no `EmpresaDashboard.js`). Confirmado via SQL (`information_schema.columns`) que a tabela `task_completions` só tem as 4 colunas enviadas (`task_id`, `completion_date`, `completed`, `completed_at`), sem risco de violar constraint.
**Build verificado:** `✓ Compiled successfully`.

### Contador "Colaboradores" → "Usuários" no card de empresa (aba Dados, master admin)
No card de cada empresa dentro da aba **Dados** (`app/admin/page.js`, `DadosTab`), o contador mostrava `r.colaboradores_count` — campo da RPC `admin_financeiro` que só soma `role = 'colaborador'`, deixando de fora gerente/supervisor/sócio daquela empresa. Trocado para `r.usuarios_count` (mesma RPC, já soma `role in ('colaborador','gerente','supervisor','socio')` — todo mundo cadastrado na empresa) e label renomeado de "Colaboradores" para "Usuários". Confirmado via SQL contra dado real: empresa ArmyBR tinha 4 colaboradores mas 7 usuários no total — o card agora mostra 7. Escopo do fix: só esse card específico (os demais usos de `colaboradores_count`/contadores "colaborador" no arquivo são de outras telas — ex: lista "Colaboradores sob gestão" de supervisor, contagem por loja — que fazem sentido continuar filtrando só colaboradores).
**Build verificado:** `✓ Compiled successfully`.

### Investigação de "faturamento total" divergente (aba Faturamento, sócio) + título sem mês
Felipe reportou R$ 108.669,24 aparecendo na aba Faturamento (herocard "Faturamento total" + tabela "Faturamento por loja") quando o esperado era R$ 107.469,84 (soma real da ArmyBR Anália Franco, única loja com movimento — a outra, Iburapuera, está zerada). Auditoria completa de `loadFaturamento` em `HierarchyHome.js` contra o banco via SQL: soma de `sales_entries.daily_amount` por loja/mês bate exatamente com R$ 107.469,84, sem duplicidade de `employee_id + entry_date`, sem `loja_id` nulo/órfão, sem entrada de papel que não seja colaborador contaminando o total — nenhum bug de cálculo encontrado no código. Encontrado, sim, que boa parte dos lançamentos de julho tinha `updated_at` muito recente (segundos/minutos entre um e outro, no exato momento da investigação) — sinal forte de que os valores estavam sendo corrigidos/editados em tempo real, e a diferença de R$ 1.199,40 é compatível com um retrato momentâneo antes dessas correções, não um erro de código. Orientado a conferir de novo após refresh; se persistir, é bug de estado/cache no front e não de cálculo. **Título "Faturamento por loja — {mês}" → "Faturamento por loja"** (sem sufixo de mês), conforme pedido.
**Build verificado:** `✓ Compiled successfully`.

### Card "Lançar valor vendido" do gerente unificado com o do colaborador
Felipe pediu que o card de lançamento de venda usado pelo gerente (aba **Metas → Lançamentos**, componente `Lancamentos` em `EmpresaDashboard.js`) seguisse exatamente a mesma lógica já aplicada ao card do colaborador (`ColaboradorView.js`), já que não faz sentido corrigir venda de um dia que ainda não aconteceu — e o gerente, ali, escolhe por qual colaborador está lançando, então a regra de folga também se aplica. Mudanças espelhadas 1:1:
- **Título:** "Corrigir / lançar valor vendido no dia" → "Lançar valor vendido" (mesmo texto do colaborador).
- **Data limitada a até ontem:** `max={yesterdayStr(todayStr())}` adicionado no input (o estado `date` já nascia em `yesterdayStr(todayStr())`, só faltava o `max`).
- **Modal de folga em venda R$0,00:** `save` dividido em `save` (handler do submit) + `doSave(isFolga)` (upsert de fato), mesmo padrão do colaborador. Quando o valor é `0`, abre modal "Foi um dia de folga?" nomeando o colaborador selecionado; se confirmado, além de salvar o lançamento de R$0, busca as tarefas ativas desse colaborador (`tasks` filtradas por `employee_id` + `active=true`) e marca como concluídas (`completed: true`) todas as que valem (`isTaskDueOn`) na data lançada — upsert em `task_completions` com `onConflict: "task_id,completion_date"`.
- Como `Lancamentos` é um componente compartilhado dentro de `EmpresaDashboard.js`, o ajuste vale automaticamente pra todo mundo que usa esse dashboard pra corrigir venda de colaborador (gerente, master_admin, supervisor/sócio quando gerenciando uma loja) — sem precisar duplicar em nenhum outro arquivo.
**Build verificado:** `✓ Compiled successfully`.

### Avatar (foto ou iniciais) na aba Colaboradores
Criado `lib/Avatar.js` — componente reutilizável que replica o padrão já usado no header (`AppShell.js`): mostra a foto cadastrada (`profiles.avatar_url`) se existir, senão um círculo com gradiente roxo/rosa e as iniciais do nome. Aplicado nas duas listas do componente `Colaboradores()` em `EmpresaDashboard.js` — "Gerentes" e "Equipe" — em todos os estados de cada linha (visualização normal, modo "ver como" com `onOpenGerente`/`onOpenEmployee`, e modo edição). Como `Colaboradores()` é compartilhado por gerente/master_admin/supervisor/sócio (regra de ouro), o avatar aparece igual pra todo mundo sem precisar duplicar em outro arquivo. `avatar_url` já vinha nas queries (`select("*")` em `profiles`), não precisou mudar nenhuma busca. Confirmado via SQL: os 4 colaboradores de teste têm foto cadastrada (testa o caminho da `<img>`) e o gerente não tem (testa o fallback de iniciais).
**Build verificado:** `✓ Compiled successfully`.

### Botões de texto ("editar"/"desativar"/"ativar"/"excluir"/"salvar"/"cancelar") viraram botões com ícone, em todo o app
Felipe pediu que qualquer link de texto que funciona como ação de linha (editar, desativar/ativar, excluir e afins) fosse substituído por botão com ícone, igual ao padrão que já existia em alguns lugares do app (Pencil/Trash2/KeyRound). Auditoria completa e conversão em:
- **`lib/EmpresaDashboard.js` → `Colaboradores()`:** listas de "Gerentes" e "Equipe" — os 4 textos (editar/redefinir senha/desativar-ativar/excluir) viraram 4 ícones lado a lado (`Pencil`, `KeyRound`, novo `Power`, `Trash2`), e o par salvar/cancelar do modo de edição virou `Check`/`X`.
- **`lib/EmpresaDashboard.js` → `Tarefas()`:** par salvar/cancelar da edição inline de tarefa virou `Check`/`X` (o editar/excluir de tarefa já era ícone).
- **`lib/EmpresaDashboard.js` → `Premiações`:** "excluir" de cada lançamento virou ícone `Trash2`.
- **`lib/EmpresaDashboard.js` → `Metas`:** "cancelar" do painel de distribuição customizada virou ícone `X` (o editar/excluir de cada meta já era ícone).
- **`lib/HierarchyHome.js` → lista de Supervisores:** "desativar"/"ativar" virou ícone `Power`.
- **`app/admin/page.js`:** botão "editar" (texto+ícone) dos dados de contato da empresa virou ícone-só; botão "Redefinir senha para 123456789" (link colorido com ícone) ganhou moldura de botão (borda + fundo no hover) — os demais (empresa ativa/inativa/excluir, editar nome, editar valores/distribuição de meta, ver como) já eram ícone.
- **Fora do escopo, deixados como estão de propósito:** "cancelar" ao lado de um botão real de submit em formulários de criação (ex: "Criar gerente"/"cancelar") — é um padrão de hierarquia primário/secundário intencional, não um botão disfarçado de texto; "Alterar senha" (ChangePassword.js) e "Sair" (AppShell.js) são toggles/nav, não ações de editar/excluir; "vincular loja"/"nova loja" são ações de adicionar, não citadas no pedido.
- Novo ícone `Power` importado em `EmpresaDashboard.js` e `HierarchyHome.js` pra representar ativar/desativar de forma consistente com o resto do app (mesmo ícone já usado pra empresa ativa/inativa em `app/admin/page.js`).
**Build verificado:** `✓ Compiled successfully`. Confirmado por grep que não sobrou nenhuma ocorrência de `>editar<`, `>excluir<`, `>desativar<` ou `>ativar<` como texto puro em nenhum arquivo `.js` do projeto.

### Campo de data do "Registrar advertência" estourando o card (afetava todos os papéis)
Felipe reportou que o campo de data em Advertências → Registrar advertência estava muito grande e cortando o dashboard na visão do sócio. Comparado com os outros 3 campos `type="date"` do app (`lib/ColaboradorView.js`, `lib/EmpresaDashboard.js` linhas 1071 e 1941), todos usam `className="input date-input"` — a classe `.date-input` (definida em `app/globals.css`) força o `font-size`/`appearance` do seletor nativo de data pra não renderizar maior que o resto do formulário (bug conhecido do Safari/Chrome mobile). Só o campo de "Registrar advertência" estava com `className="input"`, sem essa classe — causa raiz encontrada e confirmada por comparação direta com os demais. Como `Advertencias()` é um componente compartilhado dentro de `EmpresaDashboard.js`, o bug valia igualmente pra gerente, master_admin, supervisor e sócio — não era exclusivo de nenhum papel. Corrigido adicionando `date-input` na classe do input.
**Build verificado:** `✓ Compiled successfully`.

### Cards de cadastro (gerente/colaborador/supervisor) colapsáveis + Novo supervisor na aba Colaboradores do sócio
Felipe pediu que os dashboards de "cadastrar colaborador", "cadastrar gerente" etc. só mostrassem os campos ao clicar numa setinha pra baixo, igual já acontecia em "Nova empresa" (master admin) — e que o sócio também tivesse como criar supervisor direto pela aba Colaboradores (não só na aba Supervisores separada).
- **`lib/EmpresaDashboard.js` → `Colaboradores()`:** cards "Novo gerente" e "Novo colaborador" agora nascem fechados, com cabeçalho clicável (`ChevronDown`/`ChevronUp`) igual ao padrão de "Nova empresa" em `app/admin/page.js` — vale pra todo mundo que usa esse componente (master_admin, sócio, supervisor, gerente).
- **Novo componente `NovoSupervisor`** (dentro de `EmpresaDashboard.js`, antes de `Colaboradores()`): mesmo padrão colapsável, busca **todas as lojas da empresa** via `empresaId` (não só a loja selecionada, já que acesso de supervisor não é preso a uma loja só) e reaproveita a rota `/api/admin/create-hierarchy` já existente (que já permitia sócio criar supervisor, sem mudança de backend). Renderizado no topo de `Colaboradores()` só quando `viewerRole === "socio"`.
- **`lib/HierarchyHome.js` → `SupervisoresTab`:** o toggle "Novo supervisor" (que já colapsava, mas com botão de texto "cadastrar supervisor"/"cancelar" + ícone Plus/X) foi padronizado pro mesmo visual de chevron, pra ficar consistente com o resto do app.
- **Lógica de permissão confirmada/documentada** (sem mudança de código, já era assim): Master cadastra todo tipo de usuário (empresa/sócio/supervisor/gerente/colaborador, via `app/admin/page.js`); Sócio cadastra supervisor (novo, nessa aba) + gerente + colaborador (`canManageTeams` já cobria isso); Gerente só cadastra colaborador (`canManageTeams = false` esconde tanto "Novo gerente" quanto "Novo supervisor" pra esse papel — e a API `/api/admin/create-gerente` já bloqueia gerente no servidor também, checado por segurança).
**Build verificado:** `✓ Compiled successfully`.

### "Colaboradores sob gestão" (master) e referência de equipe nos formulários de cadastro
Felipe apontou que o "Colaboradores sob gestão" na visão master (dentro de uma empresa, expandindo um sócio/supervisor) devia mostrar o próximo nível da hierarquia, não colaborador direto:
- **`app/admin/page.js` → `HierarquiaList`:** trocada a variável `colaboradoresScope` por `teamScope`, com regra condicional — sócio: `role in (supervisor, gerente)` filtrado por `empresa_id` (sócio gerencia a empresa inteira); supervisor: `role = gerente` filtrado pelas lojas com `loja_access` (mesmo `scopedLojaIds` que já existia). Label também virou dinâmico ("Supervisores e gerentes sob gestão" / "Gerentes sob gestão"), e cada badge da lista mostra o papel da pessoa quando é a visão do sócio (mistura dois papéis).
- Confirmado via SQL contra dado real (empresa ArmyBR: 1 sócio, 1 supervisor, 2 gerentes, 4 colaboradores) que o novo filtro bate: sócio veria os 3 (1 supervisor + 2 gerentes), supervisor veria só os gerentes das lojas que tem acesso.

Na sequência, Felipe pediu que essa mesma lógica "funcionasse também dentro dos dashboards de cadastrar novo supervisor, cadastrar novo gerente". Auditei os formulários de criação (`AddHierarchyForm`/`AddGerenteForm` no admin, `NovoSupervisor`/"Novo gerente" no `EmpresaDashboard.js`, "Novo supervisor" no `HierarchyHome.js`) e não achei nenhuma lista de colaboradores vazando indevidamente ali — só faltava uma **referência visual de quem já existe** antes de cadastrar mais gente (o "Novo gerente" já tinha isso, via a lista "Gerentes" logo abaixo; o "Novo supervisor" novo, recém-criado, não tinha nada equivalente). Resolvido com a pergunta ao Felipe, que confirmou: adicionar lista de equipe no formulário.
- **Novo componente `SupervisoresList`** (`EmpresaDashboard.js`, só leitura — nome, usuário, quantas lojas tem acesso, status ativo/inativo): renderizado pro sócio logo depois de `NovoSupervisor` e antes de "Novo gerente", fechando o fluxo visual sócio → [Novo supervisor] → [Supervisores existentes] → [Novo gerente] → [Gerentes existentes] → [Novo colaborador] → [Equipe existente]. Gestão completa (editar/redefinir senha/lojas/ativar/excluir) continua só na aba Supervisores dedicada, pra não duplicar a mesma lógica em dois lugares.
- Lista se atualiza sozinha ao cadastrar um supervisor novo: `NovoSupervisor` agora incrementa um `supervisoresVersion` (estado em `Colaboradores()`) que a `SupervisoresList` usa como dependência do fetch, além do `onChanged()` normal que já recarregava o resto do dashboard.
**Build verificado:** `✓ Compiled successfully`.

### Regra geral: valores/textos variáveis nunca quebram linha — encolhem a fonte em vez disso
Felipe pediu uma regra de UX pra todo o app: como empresas diferentes têm metas de R$ 10.000 a R$ 1.000.000+, nenhum valor variável (moeda, percentual, contador, nome) pode quebrar em duas linhas ou vazar do card — em vez disso, o sistema precisa diminuir a fonte automaticamente até caber numa linha só. Vale pra qualquer dashboard, qualquer papel, desktop e mobile.

**Novo componente `lib/AutoFitText.js`:** mede a largura natural do texto (`scrollWidth`) contra a largura disponível do contêiner (`clientWidth`) via `useLayoutEffect` + `ResizeObserver`. Se não couber, calcula um `font-size` proporcional (com 3% de margem de segurança) que faz caber, respeitando um tamanho mínimo (`minPx`, padrão 10px) — nunca trunca nem quebra, só reduz a fonte. Reage a mudança de conteúdo e de tamanho do contêiner (inclusive resize de janela/rotação de celular). Importante: pra funcionar corretamente dentro de contêineres flex/grid, o próprio componente já sai com `overflow-hidden` — isso zera o "tamanho mínimo automático" que flex/grid dariam ao item por padrão (regra do spec CSS), permitindo que ele realmente encolha em vez de estourar o layout.

**Onde foi aplicado** (troca do antigo `break-words`, que deixava o valor quebrar em 2 linhas, pelo novo `AutoFitText`, que encolhe a fonte):
- `app/admin/page.js`: componentes `HeroStat`/`HeroStatLight` (reaproveitados em várias abas — Financeiro, Dados, Overview — o fix cobre todos os usos de uma vez); "Faturamento no período" (herocard do histórico de uma empresa); cards de empresa na aba Dados (faturamento/premiações no mês); "Quanto cobrar" (preview de precificação); rótulos do gráfico de barras "Faturamento por mês".
- `lib/GerenteView.js` e `lib/ColaboradorView.js`: "Meta de hoje" (herocard, número grande) e toda a grade de mini-stats abaixo dele (falta pra meta, dias restantes, atividades pendentes, comissão até agora, premiações, líder de vendas/posição no ranking); em `ColaboradorView.js`, também os cards individuais de cada meta ("Meta de hoje"/"Falta pra bater" por meta, na aba Metas).
- `lib/HierarchyHome.js`: mesma "Meta de hoje" + grade de mini-stats (versão sócio/supervisor); "Faturamento total" da aba Faturamento.
- **Deixado como estava, de propósito:** valores dentro de linhas de lista (ranking de vendas, histórico de lançamentos, alocação de meta por colaborador) que já usavam o padrão "nome trunca com reticências (`truncate`) + valor protegido (`shrink-0 whitespace-nowrap`)" — esse padrão já impede o valor de quebrar sem precisar encolher fonte, e trocar teria sido redundante. Texto livre (motivo de advertência, nome de colaborador numa lista) também não foi tocado — esses são parágrafos onde quebra de linha é comportamento normal e esperado, diferente de um "número/badge" que devia ficar sempre numa linha só.
**Build verificado:** `✓ Compiled successfully`.

### Redesign completo do dashboard "Sócios e supervisores" (visão master, dentro de uma empresa)
Felipe reportou que o dashboard de sócios/supervisores continuava "desorganizado e confuso" mesmo depois do fix anterior de escopo (que corrigiu só QUEM aparece em "sob gestão", não a organização visual), e pediu explicitamente pra mostrar a loja de cada gerente na lista e reorganizar tudo pra ficar "mais fácil, dinâmico e simples".

**Reescrito `HierarquiaList` (`app/admin/page.js`) do zero**, trocando o antigo layout de linha-de-texto-plana por cards por pessoa:
1. **Cada sócio/supervisor vira um card com borda** (`border border-line rounded-2xl`), cabeçalho com `Avatar` (foto ou iniciais) + nome + badge de papel + badge "senha pendente"/"inativo" quando aplicável, e um subtítulo direto ("vê toda a empresa" pro sócio, ou "N lojas vinculadas" pro supervisor) — informação que antes só aparecia depois de expandir.
2. **Botões de ação viraram ícones** no cabeçalho do card: `Eye` (ver como), `Pencil` (editar — agora abre o painel `EditUser` só sob demanda, em vez de vir sempre renderizado por baixo do painel expandido, que era um dos motivos da poluição visual), `Power` (**nova capacidade**: ativar/desativar sócio/supervisor direto dali, via `/api/admin/update-employee` com `{ employeeId, newActive }` — confirmado antes de implementar que essa rota já aceita `role in [colaborador, gerente, socio, supervisor]` com bypass pra master; **não foi adicionado botão de excluir** pra sócio/supervisor porque `/api/admin/delete-employee` só aceita `role in [colaborador, gerente]` no backend — excluir essas duas hierarquias não é suportado hoje, mudar isso não foi pedido), `Chevron` (expandir/recolher detalhes).
3. **Lista "sob gestão" agora mostra a loja de cada gerente**, que era o pedido central: trocou de badges soltos (`{nome} · gerente`) pra uma lista (`<ul className="divide-y">`) com `Avatar` pequeno + nome + `{papel} · {nome da loja}`. Nome da loja resolvido via um lookup novo (`lojaNameById`, montado a partir da prop `lojas` que a `HierarquiaList` já recebia) cruzando `c.loja_id` de cada gerente — só se aplica a gerente (supervisor não tem uma única `loja_id`, continua mostrando só o papel).
4. **Seção "Lojas com acesso"** (edição de `loja_access`, toggle ver/gerenciar, vincular/desvincular loja) manteve a mesma lógica de dados de antes, só reorganizada visualmente dentro do card expandido, com título e ícone de seção pra separar melhor de "sob gestão".
**Build verificado:** `✓ Compiled successfully` (erros de prerender do build de verificação são só falta de `.env`/`SUPABASE_URL` nesse ambiente sandbox, não relacionados ao código — mesmo padrão já visto em sessões anteriores).

### Varredura de cobertura do AutoFitText (fechando os pontos que ainda usavam `break-words` ou ficaram sem proteção)
Felipe perguntou direto se a regra "valor variável nunca quebra linha" (seção anterior) tinha sido aplicada em tudo — resposta honesta foi não, só nos pontos de maior risco. Varredura de todos os `formatBRL(...)` e nomes variáveis nos 5 arquivos principais (`EmpresaDashboard.js`, `HierarchyHome.js`, `GerenteView.js`, `ColaboradorView.js`, `app/admin/page.js`) pra achar o que ainda faltava.

**Achados e correções:**
1. **`break-words` real remanescente** (`GerenteView.js`, linha de "Distribuição da meta em jogo"): nome do colaborador ainda usava `break-words` (o anti-padrão que a regra elimina) e o valor ao lado só tinha `whitespace-nowrap`, sem `shrink-0` — trocado pro padrão já estabelecido no resto do app: nome com `truncate min-w-0`, valor com `shrink-0 whitespace-nowrap`, `<li>` sem `flex-wrap` (senão o truncate não força nada, a linha só quebra inteira).
2. **`EmpresaDashboard.js` — 4 pontos sem proteção nenhuma:** valor de premiação mensal somado no card de configurações (versão somente-leitura, dentro de um `<p className="input">`) agora usa `AutoFitText`; a mesma "Premiação mensal" na lista-resumo do leitor ganhou `shrink-0 whitespace-nowrap`; o valor da meta da loja no card de cada meta (`{formatBRL(g.store_total)}`) virou `AutoFitText`; e duas listas de linha único-valor (distribuição de meta por colaborador, e histórico de lançamentos do colaborador) que não tinham proteção nenhuma ganharam o padrão `truncate` (nome/data) + `shrink-0 whitespace-nowrap` (valor).
3. **Confirmado como correto e deixado como estava:** todo `formatBRL` dentro de `<table>` (tabelas têm scroll horizontal via `overflow-x-auto` no container, padrão já aceito antes); frases de texto livre com valor embutido ("Vendido até ontem: R$X", "N colaborador(es) · R$X cada") — quebra de parágrafo normal é esperada aí, diferente de um badge/número isolado; percentuais combinados ("X% colaborador · Y% gerente") — valor é sempre limitado a 3 dígitos, risco de overflow é baixo o suficiente pra não justificar a mudança.
**Build verificado:** `✓ Compiled successfully`.

### Bug real no AutoFitText: texto cortado (não encolhia) por causa de corrida com o carregamento da fonte
Felipe mandou print mostrando "R$ 250.000,0(" cortado no mini-stat "Falta pra meta do mês" (herocard do colaborador) — o texto estava sendo cortado pelo `overflow-hidden`, não encolhido, contrariando a regra central do componente.

**Causa raiz:** a fonte do app (Inter, via `@fontsource`) carrega de forma assíncrona. O `fit()` do `AutoFitText` roda em `useLayoutEffect` logo no mount, o que pode acontecer **antes** da Inter terminar de carregar — nesse instante o navegador mede `scrollWidth` usando a fonte de fallback do sistema, que normalmente é mais estreita. Quando a Inter troca de verdade um instante depois, os glifos (principalmente números) ficam mais largos que o medido, e o texto passa a vazar do card. Como o `ResizeObserver` só dispara quando o **próprio elemento** muda de tamanho — não quando só a fonte interna troca — nada disparava um novo cálculo, e o `overflow-hidden` (necessário pra permitir encolher dentro de flex/grid) acabava cortando o texto em vez de encolher a fonte.

**Fix (`lib/AutoFitText.js`):** adicionado um segundo gatilho de recálculo via `document.fonts.ready.then(fit)`, que roda `fit()` de novo assim que todas as fontes terminarem de carregar de verdade — corrige o cálculo já feito com a fonte errada. Também aumentada a margem de segurança do cálculo de 3% pra 5% (`* 0.95` em vez de `* 0.97`), dando mais folga contra pequenas variações de métrica de fonte/subpixel.
**Build verificado:** `✓ Compiled successfully`.

### AutoFitText continuava cortando depois do primeiro fix (font-loading) — reescrito pra medição real por convergência
Felipe confirmou que já tinha feito o deploy da correção anterior (fonte assíncrona) e o corte continuava idêntico no mesmo mini-stat ("R$ 250.000,0("). Como a fonte Inter já estava visivelmente carregada no resto da tela nesse print (títulos, botões — tudo em Inter), a suspeita de corrida com carregamento de fonte provavelmente não era a causa real (ou não era a única).

**Tentativa de reproduzir localmente pra achar a causa de verdade:** tentei montar um teste com navegador headless (Playwright/Puppeteer) num ambiente isolado pra medir `scrollWidth`/`clientWidth` de verdade — **bloqueado pela rede da sandbox** (download do binário do Chromium recusado por allowlist: "Connection blocked by network allowlist"). Sem conseguir reproduzir, não dava pra confirmar a causa exata com certeza — fui honesto sobre essa limitação em vez de inventar um diagnóstico definitivo.

**Decisão: em vez de outra estimativa (a fórmula linear antiga tinha uma premissa frágil — assume que largura de texto escala linearmente com font-size, o que não é exatamente verdade por causa de kerning/hinting/arredondamento — e além disso só calculava uma vez, confiando cegamente no resultado), reescrevi `lib/AutoFitText.js` pra fazer confirmação por medição real:**
1. Ainda faz a estimativa proporcional rápida (mesma fórmula, com 5% de margem).
2. Mas em vez de aplicar e confiar, **aplica o tamanho estimado e remede o `scrollWidth` de verdade** (isso força um reflow síncrono do navegador) — se ainda não couber, reduz 1px e remede de novo, repetindo até caber ou bater no `minPx` (limite de 24 iterações de segurança).
3. Isso garante que o tamanho final aplicado **sempre bate com a fonte que está realmente ativa naquele instante** — seja a fonte de fallback (se a Inter ainda não carregou) ou a Inter de verdade — porque não depende mais de uma fórmula estimada, e sim de medição real e repetida até convergir.
4. Mantido o gatilho de recálculo em `document.fonts.ready` (do fix anterior) como segunda camada de segurança, caso a primeira medição tenha rodado com a fonte errada.
**Build verificado:** `✓ Compiled successfully`. **Ressalva sincera:** não consegui confirmar visualmente que isso resolve 100% (rede bloqueada pra teste local, sem acesso à sessão logada do Felipe pra inspecionar o DOM ao vivo) — pedir pro Felipe conferir depois do deploy; se ainda cortar, vale investigar com o DevTools aberto no card específico (inspecionar o `<span>` do valor, ver `scrollWidth` vs `clientWidth` e se `document.fonts.status` já é `"loaded"` nesse momento).

### Bug real: aba "Supervisores" (visão sócio) quebrava ao abrir — `ChevronUp`/`ChevronDown` usados sem import
Felipe reportou erro ao clicar na aba Supervisores. Causa: no toggle colapsável "Novo supervisor" (`SupervisoresTab`, `lib/HierarchyHome.js`, linha 956), os ícones `ChevronUp`/`ChevronDown` eram usados no JSX mas **nunca foram importados** de `lucide-react` nesse arquivo — sobra de uma sessão anterior ("Padronizar toggle Novo supervisor pra chevron"). Isso derruba o componente inteiro com `ReferenceError` assim que renderiza (o `ChevronDown` já aparece incondicionalmente no cabeçalho do toggle, mesmo fechado). `npm run build` nunca pegou porque o projeto roda com **"Skipping linting"** — erro de JSX referenciando variável indefinida é só checado em runtime, não em build/type-check.
**Fix:** adicionado `ChevronUp, ChevronDown` ao import de `lucide-react` em `lib/HierarchyHome.js`.

**Auditoria extra, pra não deixar irmãos do mesmo bug escondidos:** rodei `eslint` com a regra `react/jsx-no-undef` (que o projeto não roda por padrão, já que o build pula lint) contra os 5 arquivos principais (`HierarchyHome.js`, `EmpresaDashboard.js`, `GerenteView.js`, `ColaboradorView.js`, `app/admin/page.js`) — confirmado **zero outras ocorrências** desse padrão (componente/ícone usado no JSX sem import) depois do fix. Vale considerar ligar essa regra no lint do CI/build no futuro pra pegar isso automaticamente (hoje "Skipping linting" deixa esse tipo de erro passar até alguém clicar na tela errada em produção).
**Build verificado:** `✓ Compiled successfully`.

### Removido campo de "Desconto (%)" no registro de advertência — era enganoso, não só redundante
Felipe questionou se fazia sentido ter um campo de porcentagem de desconto no formulário "Registrar advertência" (`Advertencias()`, `lib/EmpresaDashboard.js`), já que isso já é configurado globalmente no card "Configurações" da mesma aba.

**Concordei e confirmei algo pior do que redundância:** conferi o cálculo real (`calcIndividualPct`, `lib/scoring.js`) — o desconto aplicado no % individual do colaborador é sempre `quantidade de advertências do mês × penalty global da loja` (`warningsCount * penaltyPerWarning`, chamado em `EmpresaDashboard.js` linha ~215 usando `settingsRow.warning_penalty_points`). O valor de `points` gravado em cada advertência individual **nunca era lido nesse cálculo** — só era salvo no banco e mostrado na lista ("-{w.points}%"). Ou seja, quem editava essa % no registro achava que estava customizando o desconto daquela advertência específica, mas isso não tinha efeito nenhum no resultado real — o campo mentia sobre o que fazia.

**Fix:** removido o input do formulário; `addWarning` agora grava `points` automaticamente com o valor vigente de `settings.warning_penalty_points` (config global no momento do registro) — mantém a coluna do banco como registro histórico, mas sem editar de mentirinha. Adicionada uma linha informativa abaixo do formulário: "Desconto aplicado: X% (configurado abaixo)".

### Card "Configurações" (aba Advertências) virou colapsável
Felipe pediu que esse card também seguisse o padrão colapsável (setinha) já usado nos outros formulários de cadastro do app. Adicionado state `cfgOpen` (default `false`) com o mesmo padrão chevron-header — cobre tanto a visão editável (`canEditSettings`) quanto a lista somente-leitura do papel "leitor".
**Build verificado:** `✓ Compiled successfully`.

### Categoria da empresa (multi-segmento) — Fase 1: schema + cadastro (2026-07-19)
Felipe abriu uma frente grande: o Z Meta vai ganhar uma segunda vertical (**consórcio**, ao lado da vestuário atual), com fluxo de vendas totalmente diferente (funil de ligação → agendamento → venda, em vez de meta em camada por dia), e confirmou que **terão outras categorias em breve** (não é só vestuário + consórcio). Antes de implementar o CRM de consórcio em si, esta sessão entregou só a base: a empresa-cliente precisa saber a que segmento pertence, e essa base precisa ser extensível sem migração toda vez que uma categoria nova entrar.

**Decisões tomadas com o Felipe antes de codar** (registradas aqui porque vão guiar as próximas fases):
1. Motor de metas/comissão do consórcio será em **tabelas separadas** das de vestuário (não reaproveita `sales_goals`/`sales_goal_allocations`/`commission_settings`) — isola o blast radius de bug entre os dois segmentos, dado que não há testes automatizados.
2. Consórcio **mantém** tasks/checklist diário, advertências e premiações — só o mecanismo de meta de venda muda (funil em vez de camada).
3. Categorias de produto de consórcio (veículo, imóvel etc.) serão **configuráveis por empresa-cliente**, não um enum fixo.
4. Notificações push (PWA) ficam pra uma **fase separada**, depois do CRM validado em produção — exigem infra nova (service worker, VAPID, job agendado) que ainda não existe no projeto.

**O que foi implementado nesta sessão (Fase 1 — só a categoria da empresa, nada do CRM de consórcio ainda):**

1. **Tabela nova `categorias_empresa`** (`id`, `nome`, `slug`, `active`) em vez de um `CHECK` enum fixo em `empresas.categoria` — decisão direta do "teremos outras categorias em breve": adicionar uma categoria nova vira um `INSERT`, não uma migração de schema. Seed inicial: Vestuário (`vestuario`) e Consórcio (`consorcio`). RLS: leitura liberada pra qualquer usuário autenticado (é só uma lista de referência, todo profile precisa poder resolver a categoria da própria empresa pra saber que dashboard renderizar); escrita só `is_master_admin()`.
2. **`empresas.categoria_id`** (FK, `not null`) — as 2 empresas existentes (ArmyBR, Teste) foram *backfilled* automaticamente para `vestuario` antes da constraint `not null` entrar em vigor (conferido via SQL: as duas ficaram com o `categoria_id` certo).
3. **Trigger `prevent_categoria_change_after_setup`** (`BEFORE UPDATE` em `empresas`) — bloqueia trocar `categoria_id` se a empresa já tem qualquer linha em `lojas` ou `profiles`, por pedido explícito do Felipe ("uma vez que a empresa é cadastrada e já tem lojas e usuários, não pode alterar a categoria"). Testado nos dois sentidos via SQL: update em ArmyBR (já tem loja/usuário) foi rejeitado com a mensagem certa; update numa empresa vazia (criada e revertida dentro de uma transação de teste) passou normalmente. Necessário como trigger no banco, não só checagem client-side, porque master admin tem bypass de RLS em `empresas` e edita via client direto.
4. **`admin_lojas_health(p_month)` reescrita** (precisou de `DROP FUNCTION` antes do `CREATE` — mudar as colunas de retorno de uma function existente não é permitido via `CREATE OR REPLACE`, só troca de corpo com mesma assinatura de saída) — passou a trazer `categoria_id`/`categoria_nome`/`categoria_slug` de cada empresa via `LEFT JOIN categorias_empresa`. É essa RPC que alimenta toda a lista de empresas do Master Admin, então não precisou de query nova no front.
5. **Cadastro "Nova empresa" (`app/admin/page.js`)** ganhou campo obrigatório de categoria (`SelectField`, populado a partir de `categorias_empresa`). **`/api/admin/create-empresa`** passou a exigir `categoriaId`, valida que a categoria existe e está ativa antes de gravar.
6. **`EmpresaDetail`** ganhou um card "Categoria": mostra a categoria como badge; só libera edição (seletor + salvar) quando `empresa.lojas.length === 0 && empresa._colabTotal === 0` — quando travada, mostra o motivo ("já tem loja ou usuário cadastrado") em vez de simplesmente esconder o controle. A trava real é o trigger do banco; essa checagem client-side é só pra não mostrar um botão que vai dar erro.
7. **Badge de categoria também na lista "Empresas"** (visão geral do Master Admin), ao lado do nome — dá pra bater o olho e saber o segmento de cada empresa sem entrar no detalhe.

**Verificação:** `npm run build` → `✓ Compiled successfully` (erros de prerender "supabaseUrl is required" são o padrão já conhecido desse sandbox sem `.env.local`, não regressão). `eslint` com `react/jsx-no-undef` rodado manualmente em `app/admin/page.js` (dois ícones novos, `Tag`/`Lock`) — zero ocorrências. `get_advisors(security)` rodado depois de toda a migração — só a baseline já conhecida (buckets públicos, funções `SECURITY DEFINER` executáveis, incluindo a nova `prevent_categoria_change_after_setup` na mesma categoria benigna de sempre, leaked password protection). RLS de `categorias_empresa` testada simulando um colaborador real: consegue `SELECT` (esperado), tentativa de `INSERT` rejeitada pela política (esperado).

**Fora do escopo desta sessão, de propósito:** nada do funil de consórcio (`crm_leads`, categorias de produto configuráveis, motor de metas de consórcio, dashboards) foi construído ainda — só a base de categoria da empresa. Próxima fase, a combinar com o Felipe: schema do funil de ligações/agendamentos/vendas.

### Categoria da empresa (multi-segmento) — Fase 2: schema do funil de consórcio (2026-07-19)
Continuação direta da Fase 1 (categoria da empresa). Esta sessão entregou **só schema + RLS** do funil de vendas de consórcio (ligação → agendamento → resultado) e do motor de metas próprio — nenhuma tela ainda (isso é Fase 3/4).

**Tabelas novas** (todas com RLS espelhando exatamente o padrão já estabelecido no projeto, sem inventar helper novo):
1. **`consorcio_produto_categorias`** (`empresa_id`, `nome`, `active`) — categorias de produto (veículo, imóvel...) configuráveis por empresa-cliente, como decidido na Fase 1. RLS: leitura pra qualquer autenticado (`is_master_admin() OR empresa_id = my_empresa_id()`) — todo colaborador precisa ler essa lista pra preencher o modal de venda; escrita só `is_master_admin()` ou sócio da própria empresa (`is_socio() AND empresa_id = my_empresa_id()`).
2. **`crm_leads`** — a "ligação", um registro único evoluindo de status (`novo → agendado → follow_up/perdido/vendido`, `follow_up` pode voltar a ser resolvido depois), não três tabelas separadas pra ligação/agendamento/venda. Dois `CHECK` de forma (mesmo padrão de `tasks.recurrence_type`): `agendamento_at` obrigatório quando `status='agendado'`; `valor`+`categoria_produto_id` obrigatórios (e só permitidos) quando `status='vendido'`. RLS espelha `sales_entries` (colaborador lança a própria venda): `employee_id = auth.uid() OR is_master_admin() OR (is_gerente() AND is_my_team_member(employee_id)) OR can_manage_loja(loja_id)` pra insert/update; select libera também `loja_id = my_loja_id()` e `can_view_loja(loja_id)` (mesmo padrão de leitura de `tasks`/`warnings`/`sales_entries`). **Sem policy de `DELETE`** — decisão deliberada: o funil existe pra dar histórico completo ("total de ligações, quantas viraram agendamento, quantas foram perdidas..."), então não expus exclusão nenhuma pelo client: só dá pra editar o status/dados de um lead, nunca apagar. Se precisar de correção de erro de cadastro no futuro, é decisão a tomar com o Felipe (edição sim, mas exclusão de funil historicamente já foi recusada pra outras tabelas nesse projeto por esse mesmo motivo).
3. **`consorcio_goals` / `consorcio_goal_allocations` / `consorcio_commission_settings`** — espelham `sales_goals`/`sales_goal_allocations`/`commission_settings` 1:1 (mesmas colunas, mesma RLS: `can_manage_loja(loja_id)` pra escrita, `my_loja_id()`/`can_view_loja(loja_id)` pra leitura). Tabelas **separadas** das de vestuário, por decisão explícita do Felipe (isola o blast radius de bug entre os dois segmentos, dado que não há testes automatizados). A lógica pura de cálculo (`currentGoalTarget`/`achievedTier`, `lib/scoring.js`) não precisa duplicar — é agnóstica de onde vem o "vendido"; só a fonte de dado muda (`sum(crm_leads.valor)` no lugar de `sum(sales_entries.daily_amount)`), a implementar na Fase 4.

**Verificação (SQL direto no Supabase, simulando RLS como usuários reais — não só leitura de policy):**
- Fluxo completo `novo → agendado → vendido` testado como a própria colaboradora (Lais Reis, empresa ArmyBR): criação do lead, tentativa de marcar `agendado` sem `agendamento_at` (rejeitada pelo `CHECK`), marcação correta com `agendamento_at`, tentativa de marcar `vendido` sem `valor`/categoria (rejeitada), marcação correta — todos os passos bateram com o esperado.
- Isolamento: colaboradora tentando criar lead em nome de uma colega (`employee_id` de outra pessoa) — bloqueado pela RLS de insert. Colaborador de **outra empresa** tentando ler o lead — 0 linhas retornadas (isolamento multi-tenant confirmado). Gerente da loja e sócio da empresa conseguem ver/editar o lead normalmente.
- `consorcio_produto_categorias`: sócio da empresa consegue cadastrar categoria nova; colaborador tentando cadastrar — bloqueado pela RLS.
- Nenhum dado de teste ficou no banco (todos os testes rodaram dentro de transação com `rollback`; confirmado por contagem zerada nas 3 tabelas principais depois).
- `get_advisors(security)` rodado depois de toda a migração — mesma baseline já conhecida, nenhum alerta novo (as tabelas novas não criaram nenhuma function `SECURITY DEFINER` adicional).

**Fora do escopo desta sessão, de propósito:** nenhuma tela ainda. Próximas fases (a combinar): Fase 3 — `ColaboradorView` de consórcio (cadastrar ligação, lista de leads, agenda do dia, botão de resolver com os 3 modais); Fase 4 — dashboard de gestão (funil agregado, metas de consórcio); Fase 5 — notificações push (separada, depois do CRM validado).

**Pedido do Felipe pra próxima fase de UI (registrado aqui pra não esquecer, ainda não implementado):** cada usuário precisa de um dashboard com as reuniões/agendamentos do dia + seta pra navegar dia a dia, e uma aba de calendário "bonita e moderna, parecida com a da Apple" — o app precisa ser visualmente atrativo o suficiente pra dar vontade de abrir. Felipe vai mandar print de referência quando chegar a hora de implementar. Isso pertence à Fase 3 (experiência do colaborador) ou pode virar sua própria sub-fase, a decidir quando começarmos a UI.

### Categoria da empresa (multi-segmento) — Fase 3: tela do colaborador de consórcio (2026-07-19)
Primeira tela de verdade do segmento consórcio, em cima do schema da Fase 2. Escopo combinado com o Felipe antes de codar: funil de ligações + agenda do dia + calendário mensal + aba Tarefas (checklist reaproveitado do vestuário) — Metas de consórcio fica pra Fase 4 (motor de cálculo ainda não existe).

1. **Coluna nova `crm_leads.vendido_at`** (timestamptz, migração `add_crm_leads_vendido_at`). Adicionada antes de escrever a UI porque `updated_at` não serve pra saber "quando a venda aconteceu" — qualquer edição futura (corrigir observação, etc.) mexeria em `updated_at` e inflaria/deslocaria o "vendido no mês" sem motivo. `vendido_at` só é setado uma vez, no momento em que o status vira `vendido`.
2. **Roteamento por categoria (`app/colaborador/page.js`).** Depois de carregar o profile, busca `empresas.categoria_id` → `categorias_empresa.slug` (2 queries simples, sem RPC nova) e decide: `slug === 'consorcio'` renderiza `ColaboradorViewConsorcio`, senão continua com `ColaboradorView` de sempre. Abas do `AppShell` também mudam por categoria: vestuário continua Início/Metas; consórcio vira Início/Calendário/Tarefas (sem Metas ainda).
3. **Componente novo `lib/ColaboradorViewConsorcio.js`** — irmão de `ColaboradorView.js`, não uma variação dele por dentro (decisão da Fase 1/2: árvores de componente separadas por categoria, pra isolar risco do vestuário que já tem dado real rodando). Sub-componente interno `LeadRow` reaproveitado nos 3 lugares que listam lead (agenda do dia, "Minhas ligações", detalhe de dia no calendário).
   - **Aba Início:** herocard (gradiente azul, cor própria do consórcio pra não confundir com o lilás do vestuário) com ligações hoje / agendamentos hoje / em aberto / vendido no mês; card colapsável "Cadastrar ligação" (telefone e nome obrigatórios, resto opcional, exatamente os campos pedidos); "Agenda do dia" com `DateNav` **sem** `maxDate` (diferente do checklist de tarefas — agendamento é pro futuro, precisa poder navegar pra frente); lista "Minhas ligações" com toggle Em aberto/Todos.
   - **Botão de resolver (perdido/follow-up/vendido):** aparece em qualquer lead que não esteja em estado terminal (`vendido`/`perdido`) — inclusive um lead que nunca teve agendamento, e inclusive um que já está em `follow_up` (pode ser resolvido de novo depois). Perdido/follow-up abrem modal só de feedback; vendido abre modal de valor + categoria (lista de `consorcio_produto_categorias` da empresa) + observações, e é esse `valor`/`vendido_at` que vai alimentar o motor de metas na Fase 4.
   - **Ação extra não pedida explicitamente mas necessária: "Agendar/reagendar".** Um lead pode nascer sem agendamento (`status='novo'`) e ganhar um depois, ou um agendamento pode precisar mudar de data — ícone de calendário ao lado dos 3 botões de resolver abre um modal de data+hora que seta `agendamento_at` e move o status pra `agendado`.
   - **Aba Calendário:** grid de mês (cabeçalho com nome do mês + botão "Hoje" + `MonthNav` **sem** `maxMonth`, já que precisa navegar pra meses futuros), cada dia com até 2 chips coloridos por status (mesma paleta de badge: cinza/azul/amarelo/vermelho/verde) + "+N" quando estoura, dia atual destacado com círculo rosa (adaptação da referência que o Felipe mandou — círculo vermelho do Apple Calendar — pra cor de destaque já usada no app). Clicar num dia abre um painel de detalhe abaixo do grid com a lista completa daquele dia (mesmo `LeadRow`, mesmas ações) — não navega de aba, fica tudo dentro do Calendário.
   - **Aba Tarefas:** só o card de checklist (mesma lógica exata de `isTaskDueOn`/semeadura/`DateNav` com `maxDate` de `ColaboradorView.js`, sem duplicar regra nova) — sem barra individual/geral nem advertências nessa aba por ora, escopo combinado como "checklist" na pergunta feita antes de construir.
4. **Fora do escopo desta fase, sinalizado:** os fluxos de "ver como colaborador" (a partir do gerente ou do master admin) ainda apontam sempre pra `ColaboradorView` (vestuário), não checam categoria — um colaborador de consórcio visto "como" por um gestor hoje veria a tela errada. Isso só importa de verdade na Fase 4 (dashboard de gestão), quando essas telas de impersonation também precisam ficar categoria-aware; não mexido agora pra não misturar escopo.

**Verificação:** `npm run build` → `✓ Compiled successfully` (mesmo padrão de erro de prerender já conhecido, sem `.env.local` no sandbox). `eslint react/jsx-no-undef` no arquivo novo e em `app/colaborador/page.js` — zero ocorrências. Simulado via SQL, com o payload **exato** que o código monta (não só a forma da tabela): insert de ligação já agendada como a colaboradora Lais Reis, depois resolução como "vendido" com `valor`/`categoria_produto_id`/`vendido_at` — bateu certo nos dois passos, sem sobrar dado de teste no banco (tudo dentro de transação com rollback).

**Pendência prática pro Felipe testar de verdade:** não criei nenhuma empresa/colaborador de teste de consórcio — a criação de usuário precisa passar pela rota oficial (`/api/admin/create-employee`, que chama `admin.auth.admin.createUser` com senha) pra não arriscar um usuário quebrado no Supabase Auth criado por fora do fluxo. Pra ver a tela nova: Master Admin → Nova empresa → categoria **Consórcio** → criar uma loja → criar um colaborador → logar como esse colaborador.

### Categoria da empresa (multi-segmento) — Fase 4: dashboard de gestão + exportação de leads (2026-07-19)
Fechou o lado de gestão do consórcio (gerente/supervisor/sócio/master admin) — antes desta fase só o colaborador tinha tela própria (Fase 3). Pedido explícito do Felipe antes de criar a primeira empresa de teste: "antes de criar uma nova empresa pra testar todo o fluxo, temos que concluir a implementação".

1. **`lib/EmpresaDashboard.js` ganhou 5 exports novos, zero mudança de comportamento**: `SubNav`, `Colaboradores`, `Tarefas`, `Advertencias`, `Premiacoes` passaram de função privada pra `export function` — são pure CRUD, sem nenhuma dependência de `sales_entries`/`sales_goals` (só `tasks`/`warnings`/`employee_prizes`/`profiles`), então dá pra reaproveitar direto no dashboard de consórcio em vez de duplicar ~900 linhas. Único caso, até agora, em que o princípio "sempre bifurcar árvore de componente por categoria" (decidido nas Fases 1-3) abre exceção deliberada — só pra esses 5, que são agnósticos de categoria por natureza.
2. **`lib/ConsorcioDashboard.js` (novo)** — equivalente de `EmpresaDashboard.js` pro consórcio. Duas abas (`atividades`/`metas`, mesmas chaves de `EMPRESA_TABS`, exportado como `CONSORCIO_TABS`):
   - **Funil** (sub-aba dentro de Início, no lugar de Placar): 4 cards de agregado do dia/mês (ligações hoje, agendamentos hoje, em aberto, vendido no mês) + tabela "Funil por colaborador" com exatamente as métricas que o Felipe pediu (ligações, agendados, follow-up, perdidos, vendidos, valor vendido, e dentro de agendados a quebra "quantos venderam / quantos perderam"). Card colapsável "Categorias de produto" (CRUD de `consorcio_produto_categorias`) — sem isso não existia NENHUMA tela pra cadastrar categoria de produto, o que travava o modal de "vendido" do colaborador (dropdown vazio).
   - **Metas**: cards de meta em camada (`consorcio_goals`, progresso via `currentGoalTarget`/`ProgressBar`, mesmo padrão visual de vestuário), formulário colapsável "Nova meta" (distribuição sempre igual entre colaboradores ativos — ver simplificação assumida abaixo), configuração de comissão de não-atingimento, lista somente-leitura "Vendas do mês" (fonte `crm_leads` com `status='vendido'`).
   - **Exportar leads em Excel** (pedido do Felipe no meio desta sessão): botão na aba Funil gera um `.xlsx` (biblioteca `xlsx`/SheetJS, `import()` dinâmico só quando clicado, pra não pesar o bundle inicial) com 2 planilhas — "Leads" (linha a linha, todos os campos do cadastro) e "Resumo por colaborador" (mesmas métricas da tabela de Funil). Escopado pelos mesmos `employees` que o viewer já enxerga (ver bug de escopo corrigido no item 6). Adicionado `xlsx` ao `package.json`.
3. **`lib/GerenteViewConsorcio.js` (novo)** — espelha `lib/GerenteView.js` 1:1 na estrutura (herocard + `ConsorcioDashboard` + cards de apoio), trocando a fonte de dado de vendas: em vez de `sales_entries`/`sales_goals`, usa `crm_leads`/`consorcio_goals`. Herocard com gradiente azul (mesma paleta já usada no herocard do colaborador de consórcio, pra manter identidade visual do segmento) mostrando ligações hoje / agendamentos hoje / atividades pendentes (100% reaproveitado, é cálculo de tarefa, agnóstico de categoria) / comissão até agora / premiações / líder de vendas. `app/gerente/page.js` agora resolve a categoria da empresa (mesmo padrão de 2 queries de `app/colaborador/page.js`) e escolhe `GerenteView` ou `GerenteViewConsorcio`.
4. **Master Admin (`app/admin/page.js`)** — os três pontos que só sabiam renderizar componente de vestuário agora checam `empresaDetail?.categoria_slug` (já carregado desde a Fase 1, não precisou de query nova):
   - Loja aberta via "Ver dados da loja" → `ConsorcioDashboard` no lugar de `EmpresaDashboard` quando a empresa é consórcio.
   - "Ver como" gerente/colaborador → `GerenteViewConsorcio`/`ColaboradorViewConsorcio` quando consórcio. Precisou de um conjunto de abas próprio pra colaborador de consórcio (`TABS_CONSORCIO_COLAB`: Início/Calendário/Tarefas, sem Metas) porque o estado solto `viewTab` (reaproveitado entre "ver comos" de pessoas diferentes) podia carregar uma chave de aba inválida pro conjunto novo (ex.: `"metas"` sobrando de uma visita anterior a um gerente) — corrigido com uma validação (`effectiveViewTab`) que cai pra `"atividades"` se a chave não existir no conjunto de abas da pessoa/categoria atual.
5. **`lib/HierarchyHome.js` (sócio/supervisor) — versão mínima, por decisão de escopo**: categoria é resolvida uma vez (é da empresa, não da loja — todas as lojas de um sócio/supervisor compartilham a mesma categoria). Quando consórcio: herocard troca pra versão simplificada (ligações/agendamentos/pendentes/líder, fonte `crm_leads`, função nova `loadHeroConsorcio`), `EmpresaDashboard` vira `ConsorcioDashboard`, e os cards de "Metas da loja"/"Ranking de vendas" (baseados em `sales_goals`/`sales_entries`) somem — o próprio `ConsorcioDashboard` já cobre isso na aba Metas. **Simplificação assumida e não escondida**: as abas Rankings e Faturamento (cruzam todas as lojas do sócio/supervisor) continuam 100% baseadas em `sales_entries`/`sales_goals`/`commission_settings` — pra uma empresa de consórcio elas mostrariam zero de forma enganosa, então em vez de portar os ~300 linhas de agregação cross-loja (`loadRankings`/`loadFaturamento`) nesta fase, essas duas abas mostram um aviso "ainda não adaptado pra consórcio, em breve" quando a empresa é consórcio. Os números por loja já aparecem na aba Início.
6. **Bug de escopo pego durante a implementação, não reportado pelo Felipe**: o `loadAll` do `ConsorcioDashboard` busca `crm_leads` da loja inteira (`eq("loja_id", lojaId)`), mas pra um **gerente** o array `employees` já vem filtrado só pra própria equipe (`eq("gerente_id", viewerId)`, mesmo padrão de `EmpresaDashboard`). Sem escopar os leads pelos mesmos `employees`, um gerente veria os números da loja toda (de todas as equipes) nos cards do topo, na tabela de Funil e no relatório Excel — não só da própria equipe. Corrigido filtrando `leads` por `employees` (`scopedLeads`) antes de qualquer agregação.
7. **Migração `add_consorcio_commission_settings_unique`**: `consorcio_commission_settings` estava sem a constraint `UNIQUE(loja_id, month)` que a tabela espelhada de vestuário (`commission_settings`) tem — gap real da migração da Fase 2 (mirror não ficou 100% igual). Sem isso, o `upsert({ onConflict: "loja_id,month" })` da tela de configuração de comissão do consórcio quebraria em runtime ("no unique or exclusion constraint matching the ON CONFLICT specification"). Pego e corrigido antes de qualquer tela chamar esse upsert de verdade.
8. **Bug de permissão pego durante a implementação**: a RLS de `consorcio_produto_categorias` só libera escrita pra `is_master_admin()` ou sócio da própria empresa (`is_socio() AND empresa_id = my_empresa_id()`) — supervisor não entra, porque categoria de produto é configuração da empresa inteira, não de uma loja específica (supervisor é escopado por loja via `loja_access`). O botão "Adicionar categoria" no `ConsorcioDashboard` foi restrito a `master_admin`/`socio` (`canManageProdutoCategorias`) pra não mostrar uma ação que ia falhar silenciosamente pra supervisor.

**Simplificações assumidas nesta fase, registradas com honestidade pro Felipe (nenhuma escondida no código sem comentário):**
- Distribuição de meta de consórcio é sempre igual entre os colaboradores ativos da loja no momento da criação — sem distribuição customizada por pessoa, e sem recálculo automático quando um colaborador entra/sai da equipe depois (a versão vestuário também tem uma lógica de recálculo automático mais sofisticada que não foi replicada aqui).
- Gestão (gerente/supervisor/sócio/master) não edita leads de colaborador — só o próprio colaborador resolve os próprios leads. Se precisar corrigir erro de cadastro em nome de alguém, é decisão de produto a tomar com o Felipe antes de implementar (mesma lógica já aplicada a `crm_leads` não ter `DELETE` na Fase 2).
- Rankings/Faturamento cross-loja do sócio/supervisor não cobrem consórcio ainda (item 5 acima).

**Verificação:** `npm install` + `npm run build` → `✓ Compiled successfully` (mesmo padrão de erro de prerender já conhecido, sandbox sem `.env.local`). `eslint react/jsx-no-undef` rodado em todos os arquivos tocados (`ConsorcioDashboard.js`, `GerenteViewConsorcio.js`, `HierarchyHome.js`, `app/admin/page.js`, `app/gerente/page.js`) — zero ocorrências (os únicos erros do lint stripped foram avisos de "regra react-hooks/exhaustive-deps não encontrada" nos comentários `eslint-disable-next-line`, não relacionados a `jsx-no-undef`). `get_advisors(security)` rodado depois da migração da unique constraint — mesma baseline já conhecida, nenhum alerta novo. Não há empresa de consórcio real no banco ainda pra testar contra dado de produção (confirmado via SQL: as duas empresas existentes, ArmyBR e Teste, são vestuário) — a criação da primeira empresa de teste é o próximo passo do Felipe, agora que a implementação está completa.

### Ajustes finos pós-Fase 4 + confirmação global de "salvo" + calendário do gerente (2026-07-19)

Sequência de pedidos pontuais do Felipe depois que a Fase 4 (dashboard de gestão do consórcio) foi dada como concluída — cada um tratado como mudança isolada, com build+lint verificados a cada bloco.

1. **`lib/ColaboradorViewConsorcio.js` — esconder botões de resolver em "Minhas ligações"**: `LeadRow` ganhou prop `showResolve` (default `true`); só a lista "Minhas ligações" passa `showResolve={false}` — resolver (perdido/follow-up/vendido) só faz sentido a partir da Agenda do dia/Calendário, "Minhas ligações" vira consulta/histórico (mas ainda permite agendar/reagendar).
2. **Distribuição de meta em jogo no supervisor/sócio — adicionada e depois revertida**: pedido inicial do Felipe ("adicionar campo de distribuição de meta na visão do supervisor"), implementado em `lib/HierarchyHome.js`, depois revertido no mesmo dia por instrução explícita ("na verdade tire do supervisor e sócio... deixe só no gerente mesmo, pra ficar igual ao vestuário"). Ficou só em `lib/GerenteViewConsorcio.js` (já existia lá desde a Fase 4).
3. **Auditoria de cards colapsáveis em todo o app** ("encontre todos os mais colapsados e corrija" — só a setinha era clicável em vez do card inteiro): varredura por grep de `ChevronDown`/`ChevronUp`/`isOpen` em `lib/`+`app/`. Achado 1 bug real: `LojaCard` (dentro de `EmpresaDetail`, `app/admin/page.js`) tinha o bloco de info da loja como `<div>` estática, só o ícone chevron abria/fechava — convertido pra `<button className="text-left flex-1 min-w-0 hover:opacity-75...">`. Todos os outros ~15 padrões checados já eram full-width/full-row clicáveis.
4. **Filtro de data + status na exportação de leads em Excel** (`lib/ConsorcioDashboard.js`, componente `Funil`): modal próprio (`exportModalOpen`) com 2 campos de data (`data_ligacao`, opcionais) e checkboxes de status (novo/agendado/follow_up/perdido/vendido). As duas planilhas exportadas ("Leads" e "Resumo por colaborador") são recalculadas a partir do MESMO conjunto filtrado, pra não ficar inconsistente entre si.
5. **Exportar Excel restrito a gerente** — pedido inicial "aparecer só pra gerente/supervisor/sócio", revertido no mesmo dia pra "só gerente mesmo, igual ao vestuário" (vestuário nunca teve exportação pra supervisor/sócio). `canExport = viewerRole === "gerente"` (excluindo master_admin também, por instrução explícita anterior).
6. **Modal global de confirmação "Alterações salvas"** — pedido: "em todos os botões de SALVAR... precisa aparecer um modal dizendo que as alterações foram salvas". Construído como Context+hook único (`lib/SavedNotice.js`, `SavedNoticeProvider`/`useSavedNotice()`), montado uma vez em `lib/AppShell.js` (envolve toda tela autenticada), evitando duplicar estado de modal em cada componente — qualquer descendente chama `useSavedNotice()()` com uma linha. Aplicado em: `lib/EmpresaDashboard.js` (edição de colaborador/gerente, tarefas, advertências+config, premiações, metas+comissão+prêmio mensal, lançamento manual do gerente), `lib/ConsorcioDashboard.js` (categorias de produto, metas, comissão de não-atingimento), `lib/ColaboradorViewConsorcio.js` (cadastrar ligação, resolver lead, agendar), `lib/HierarchyHome.js`+`app/admin/page.js` (categoria/nome/contato da empresa, criar empresa/loja, financeiro, criar loja, editar nome/username de usuário), `lib/EditProfile.js` (nome, usuário de login) e `lib/ChangePassword.js` (troca de senha, só no fluxo não-forçado). **Critério de exclusão deliberado, comunicado com transparência (não confirmado por Felipe ainda)**: ações que já mostram dado crítico inline que o modal cobriria (usuário/senha temporária ao criar conta, `resetPassword`), toggles instantâneos de 1 clique sem "salvar" distinto (ativar/desativar, checklist de tarefa, permissão de loja do supervisor, avatar/logo no select do arquivo) e o lançamento diário de venda do PRÓPRIO colaborador em `lib/ColaboradorView.js` (`doSaveEntry`/`saveEntry`) — ação de altíssima frequência que já tem feedback inline leve (`entryMsg`); um modal bloqueante toda vez atrapalharia mais do que ajudaria. Se Felipe discordar de algum desses casos, é ajuste pontual.
7. **Auditoria mobile dos arquivos de consórcio**: revisão de `lib/ConsorcioDashboard.js`, `lib/ColaboradorViewConsorcio.js`, `lib/GerenteViewConsorcio.js` procurando tabelas sem scroll horizontal, grids sem breakpoint, e modais sem limite de altura. Achados 2 modais reais sem `max-h`/`overflow-y-auto` que podiam estourar a viewport em tela curta (celular em paisagem, ou muitos campos): modal de exportação de Excel (`ConsorcioDashboard.js`, 2 datas + 5 checkboxes de status) e modal "Marcar como vendido" (`ColaboradorViewConsorcio.js`, 3 campos incl. textarea) — ambos ganharam `max-h-[85vh] overflow-y-auto` no card, mesmo padrão já usado no painel de `EditProfile.js`. O resto (tabela do Funil já tinha `overflow-x-auto`+`whitespace-nowrap`, grid do calendário já usava `gap-1 sm:gap-2`, heroicards já usavam `grid-cols-2 sm:grid-cols-4`) já seguia os padrões mobile-first estabelecidos — nenhum outro problema real encontrado.
8. **Aba Calendário do gerente (consórcio) + agenda no Início**: pedido "o gerente também precisa ter uma aba calendário, com todas as reuniões de todos os colaboradores... e precisa ter o dashboard na tela de início também". Implementado só em `lib/GerenteViewConsorcio.js` (não em `ConsorcioDashboard.js`, que é compartilhado com supervisor/sócio/master — o pedido foi especificamente do gerente):
   - Novo export `GERENTE_TABS` (Início/Calendário/Metas) — substitui `CONSORCIO_TABS` só nos 3 pontos que renderizam a tela do gerente: `app/gerente/page.js` (rota própria), `lib/HierarchyHome.js` (supervisor/sócio "vendo como" gerente) e `app/admin/page.js` (master "vendo como" gerente) — regra de ouro de UI (tela idêntica independente de quem está vendo).
   - Aba Calendário: grid mensal reaproveitando o padrão visual do calendário do colaborador (`buildCalendarGrid`, duplicado localmente por decisão deliberada de isolar risco, mesmo padrão já usado entre `ColaboradorView`/`ColaboradorViewConsorcio`), mas agregando `crm_leads.agendamento_at` de TODOS os colaboradores ativos da equipe (`teamEmps`), com cada compromisso identificado por "Primeiro nome: lead" no grid e nome completo no detalhe do dia selecionado.
   - Card "Agenda de hoje — equipe inteira" no topo do Início (logo abaixo do herocard), sempre visível (não preso a nenhuma sub-aba), com hora/status/nome do lead/telefone e o colaborador responsável.
   - Fonte de dado nova: `loadCalendar` (mês navegável, independente do `MonthNav` do Início) e `todayMeetings` (calculado dentro do `loadStats` já existente, mesmo guard `monthArg === mês atual` já usado pra `pendingToday`). Corrigido um bug de sincronismo de efeito durante a implementação (não chegou a rodar em produção): o primeiro rascunho disparava `loadCalendar` num `useEffect` chaveado por `[calMonth, teamEmps]`, mas `teamEmps` muda de referência (via `setTeamEmps`) ANTES de `didInit.current` virar `true` no efeito de mount — o efeito rodava uma vez guardado (`didInit` ainda falso) e nunca mais, porque `teamEmps` não muda de novo sozinho depois. Corrigido guardando o `empIds` mais atual num `useRef` (`teamEmpIdsRef`, atualizado dentro do próprio `loadStats`) e chaveando o efeito só em `[calMonth]`, chamando `loadCalendar` também explicitamente logo após `didInit.current = true` no efeito de mount.

**Verificação:** `npm install` + `npm run build` → `✓ Compiled successfully` em cada bloco (erros de prerender de sempre, sandbox sem `.env.local`). `eslint react/jsx-no-undef` rodado nos arquivos tocados a cada bloco — zero ocorrências reais (únicos erros do lint minimalista foram sobre a regra `react-hooks/exhaustive-deps` não estar carregada nesse config reduzido, referente aos comentários `eslint-disable-next-line` já existentes no padrão do projeto, não relacionado a `jsx-no-undef`). Não houve teste funcional contra dado real via SQL nesta rodada (nenhuma tabela/coluna nova, nenhuma migração) — só leitura de código e build. **Ainda não testado em dispositivo físico real** (auditoria mobile item 7 é 100% estática, como já registrado na seção 13).

### Dashboard de conversão (ligação → agendamento → venda), por hierarquia (2026-07-19)

Pedido do Felipe: "dashboard para conversão das ligações e conversão dos agendamentos... tanto para todos os usuários obedecendo a hierarquia". Como ele explicitamente pediu a lógica por escrito ANTES de qualquer código, a proposta foi apresentada via `AskUserQuestion` (3 decisões: onde exibir, como tratar agendamento em aberto, período padrão) e só depois de aprovada (opção recomendada em todas as três) virou código.

**Cálculo (mesmo em todos os níveis de hierarquia):**
- **Ligação → Agendamento** = `leads com agendamento_at preenchido ÷ total de leads` no período.
- **Agendamento → Venda** = `leads com status='vendido' ÷ leads com agendamento_at preenchido`, MESMA safra.
- **Funil completo** = `vendido ÷ total de leads` (equivale à multiplicação das duas taxas acima).
- **Cohort por `data_ligacao`** (não por `agendamento_at`/`vendido_at`) em todas as três — garante que as três taxas fecham entre si dentro do mesmo período, sem misturar safras diferentes.
- Agendamentos "em aberto" (nem vendido, nem perdido) contam no denominador de agendamento→venda (decisão confirmada com o Felipe) — card mostra quantos estão em aberto como contexto, pra não parecer que a taxa de um mês recente é ruim só por lead ainda não ter fechado.
- Período padrão: mês (reaproveita o `MonthNav`/`month` já usado em Metas/Início — sem controle novo).

**Onde aparece** (opção escolhida: "card na aba Funil + Início", em todos os níveis, cada um com seu próprio escopo de dado — mesmo padrão de `scopedLeads`/`empIds` já usado em todo o resto do consórcio):
- `lib/ConsorcioDashboard.js` (`Funil`) — card "Conversão" logo abaixo dos 4 cards de topo, visto por gerente/supervisor/sócio/master (é o componente compartilhado).
- `lib/GerenteViewConsorcio.js` — card equivalente no Início, escopado à equipe (usa o `leadRows` que `loadStats` já buscava sem filtro de data, só adicionou o filtro de cohort por mês em cima do que já vinha do banco — nenhuma query nova).
- `lib/ColaboradorViewConsorcio.js` — card "Minha conversão" no Início, escopado aos próprios leads.
- `lib/HierarchyHome.js` — card no Início do sócio/supervisor, escopado à loja selecionada (dentro de `loadHeroConsorcio`, mesmo padrão).
- `formatPct` (já existia em `lib/scoring.js`, usado em barras de progresso) reaproveitado em vez de formatar `%` na mão em cada arquivo.

**Verificação:** `npm run build` → `✓ Compiled successfully`. `eslint react/jsx-no-undef` rodado em todos os 11 arquivos tocados na sessão inteira (incluindo os desta feature) — zero ocorrências reais. Não testado contra dado real (nenhuma empresa de consórcio em produção ainda, mesma limitação já registrada na Fase 4).

## 12. Funcionalidade recusada (em aberto, sem follow-up do Felipe)

Felipe perguntou se o master_admin poderia **ver as senhas cadastradas** de cada usuário. Foi recusado com justificativa técnica (senhas ficam com hash bcrypt via Supabase Auth, irreversível; armazenar em texto puro seria antipadrão grave de segurança, com risco real de vazamento e responsabilidade legal — ainda mais relevante porque o Z Meta será vendido a outras empresas). Alternativa proposta (permitir ao master definir uma senha temporária customizada no reset, em vez de sempre a senha padrão fixa `123456789`) — **nunca construída nem confirmada por Felipe**. Não fazer nada aqui a menos que ele volte a tocar no assunto.

## 13. Coisas a saber / possíveis próximos passos

- O app não tem testes automatizados — toda verificação é manual (build + leitura de código + advisors do Supabase).
- Não há ambiente de staging conectado ao Claude — a única verificação local possível é `npm run build` num diretório sandbox; teste visual real em dispositivo mobile depende do Felipe abrir o app depois do deploy.
- A auditoria mobile foi 100% estática (leitura de código + padrões defensivos de CSS) — não houve captura de tela real de um dispositivo físico. Vale pedir pro Felipe testar em um iPhone/Android real e reportar qualquer quebra visual específica.
- Não há CHANGELOG nem versionamento semântico — o controle de progresso vem sendo feito via lista de tarefas da sessão do Claude (efêmera) e mensagens de commit. **Este documento (`CONTEXTO_PROJETO.md`) é a fonte de verdade persistente.**

## 14. Convenções de comunicação com o Felipe

- Felipe é direto, não gosta de burocracia nem textão. Respostas devem ser objetivas, sem enrolação.
- Ele pede pra ser tratado com espírito crítico — apontar pontos fracos/cegos, não só concordar (mais relevante em decisões de negócio/estratégia do que neste projeto técnico especificamente, mas vale mesmo aqui: se uma solicitação tiver um jeito melhor de ser feita tecnicamente, dizer isso antes de simplesmente executar).
- Ele mesmo roda os comandos de git (`add`/`commit`/`push`) — o Claude só entrega prontos pra copiar/colar depois de verificar o build.
- Quando uma correção de bug for na verdade um padrão repetido em vários lugares do código (como o bug do CSS ou o padrão de RLS), vale resolver na raiz/no componente compartilhado e avisar quantos lugares isso afeta, em vez de corrigir só o caso reportado.

---

**Instrução pro Claude que abrir este documento em um novo chat:** leia este arquivo por completo antes de qualquer alteração no projeto. Ao final de qualquer sessão de trabalho relevante, atualize a seção 11 (histórico) e, se necessário, as seções 8 (padrões mobile), 9 (schema) ou 12/13 (pendências), pra manter este documento como fonte de verdade viva do projeto.
